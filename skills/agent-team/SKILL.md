# Agent Team — Hierarchical Engineering Team Simulation (HETS)

Reusable toolkit primitive for orchestrating multiple agents in a tree structure that mimics a real software engineering team: PM → Senior → Mid → Junior. Each level has scoped responsibilities, contracts that must be satisfied, and budget allocations. Trust builds incrementally so high-trust roles get spot-checked rather than fully reviewed.

## When to use HETS

- Multi-step tasks that decompose naturally into parallel sub-tasks
- Tasks where verification matters (not just output, but approach quality)
- Tasks where you'd benefit from review at multiple abstraction levels
- Audits, code reviews, large refactors, multi-file features

## When NOT to use HETS

- Single-actor tasks (just spawn one Agent directly)
- Tasks under 30 minutes (the orchestration overhead exceeds the work)
- Tasks where a single perspective is correct (no review needed)

## The role hierarchy

| Real-world role | Agent role | Depth | Verifies | Budget |
|----------------|-----------|-------|----------|--------|
| Product Manager | super-agent | 0 | Cross-area patterns, business outcome | Uses parent context |
| Senior Engineer | orchestrator | 1 | Architectural soundness of children | 8K tokens |
| Mid Engineer | sub-orchestrator | 2 | Implementation quality of children | 8K tokens |
| Junior Engineer | actor | 2-3 | Functional outputs match contract | 30K tokens |

**Recursion limit**: `max_depth = 3` by default. At depth 3, only actors can be spawned.

## The triple contract (anti-1000-zeros defense)

Each agent has THREE contracts, validated by `scripts/agent-team/contract-verifier.js`:

### 1. Functional contract — did you produce required outputs?
- Output structure (frontmatter, sections)
- Minimum quantities (≥N findings, ≥M citations)
- Required fields populated

### 2. Anti-pattern contract — did you avoid known shortcuts?
- No paraphrasing of prior runs (text similarity check)
- No template repetition across child outputs
- All claims have evidence markers (`file:line`, `verified by`)
- No padding without acknowledgment

### 3. Pattern contract (H.2.7 — shipped) — did you use correct approach?
- **`noUnrolledLoops`**: scans code blocks in actor findings; ≥5 identical lines = fail (catches manual unrolling)
- **`noExcessiveNesting`**: brace-counting depth check on code blocks; default `maxDepth: 4` (C-family only — Python's indentation-based nesting is a documented limitation)
- See [patterns/structural-code-review.md](patterns/structural-code-review.md) for the design + when-to-use guidance

The 1000-zeros example now fails Contract #3 cleanly: the unrolled `print(0)` × 1000 trips `noUnrolledLoops` at the 5th repetition.

## Workflow

### 1. Pre-run: load contracts and budgets
For each agent role you'll spawn, locate its contract file (e.g., `swarm/personas-contracts/01-hacker.contract.json`). The contract specifies:
- Token budget (with extension policy)
- Required functional outputs
- Anti-pattern checks
- Acceptable fallback behaviors

### 2. Spawn with explicit contract
Each spawned agent receives in its prompt:
- Its frontmatter contract (id, role, depth, parent, max_depth)
- Reference to its contract file (`see X.contract.json`)
- Token budget and extension policy
- Required outputs schema

### 3. Track in tree
The `tree-tracker.js` records every spawn:
```bash
node scripts/agent-team/tree-tracker.js spawn \
  --run-id chaos-... --parent super-root \
  --child orch-code --task "..." --role orchestrator
```

This persists to `swarm/run-state/{run_id}/tree.json` with full audit trail (spawn time, parent, status).

### 4. Agent completes → contract verifier runs
After each agent finishes, the parent (or super agent) invokes:

```bash
node scripts/agent-team/contract-verifier.js \
  --contract swarm/personas-contracts/01-hacker.contract.json \
  --output swarm/run-state/{run_id}/node-actor-hacker.md \
  --previous-run swarm/run-state/{prior_run_id}
```

Returns JSON with:
- `verdict`: pass | partial | fail
- Per-check status (functional, anti-pattern)
- Recommendation: accept | retry-with-tighter-prompt | escalate-to-orchestrator

### 5. On failure: retry once with tighter prompt
If verdict is `fail` and the agent has retry budget, the parent re-spawns with:
- A clearer task description
- Specific failed check ids highlighted
- Reduced scope if budget is tight

If retry also fails: escalate to orchestrator. The orchestrator may either accept-with-caveat (mark in their findings) or downgrade the trust score for that persona.

### 6. Self-learning hook
Every contract verification result is appended to `~/.claude/agent-patterns.json` via `pattern-recorder.js`:

```json
{
  "task_signature": "<hash of task>",
  "agent_role": "actor",
  "persona": "01-hacker",
  "verdict": "pass",
  "checks_passed": ["F1", "F2", "F3", "A1", "A3"],
  "checks_failed": ["A2"],
  "evidence_quality": 0.85,
  "tokens_used": 28400,
  "tokens_extended": false,
  "ran_at": "2026-05-02T..."
}
```

This feeds the existing `/self-improve` workflow so users can see WHICH agent patterns succeed across runs and WHICH consistently fail.

### 7. Super agent reviews
After all leaves complete and contracts verify:
- Super agent runs the aggregator at `~/Documents/claude-toolkit/swarm/hierarchical-aggregate.js` — **deliberately at `swarm/` (not `scripts/agent-team/`) because it's chaos-test runtime tooling, not HETS substrate**: it reads `swarm/run-state/<run-id>/node-*.md` files produced by chaos-test runs and is only invoked by the chaos-test workflow (`super-agent.md`, `orchestrator.md`, `commands/chaos-test.md`). Substrate scripts at `scripts/agent-team/` are consumed by spawned actors + verifier callsites; the aggregator is consumed only by the chaos-test workflow itself. **DECIDED in H.5.5** after 5 chaos runs flagged the location: location is correct; framing was wrong.
- Reviews orchestrator-level synthesis
- Spot-checks actor outputs based on trust scores (Phase H.2)
- Writes consolidated report
- Triggers cross-run delta analysis

## Inheritance from MiroFish (and beyond)

| MiroFish concept | HETS analog |
|------------------|-------------|
| Multi-platform parallel sim | Multi-orchestrator parallel decomp |
| `simulation_manager.py` | `tree-tracker.js` + orchestrator skill |
| `report_agent.py` | `super-agent.md` + `hierarchical-aggregate.js` |
| Static personality | Trust-evolving role contracts |
| Run state tracking | `tree.json` + per-node `node-{id}.md` files |

The HETS pattern goes further than MiroFish in three ways:
1. **Verifiable contracts** — every agent must satisfy functional + anti-pattern checks
2. **Recursion with limit** — orchestrators can spawn sub-orchestrators (bounded by max_depth)
3. **Trust-based delegation** (Phase H.2) — high-trust roles get spot-checked instead of fully reviewed

## Anti-pattern: the 1000-zeros problem

Stated by user: *"a sub-agent is asked to create a program to print a 1000 0s. We'd expect a loop. But, it's just as likely that printing 0 a thousand times could pass the test but is architecturally wrong."*

The defense:
- **Functional contract** would pass either solution (output is 1000 zeros)
- **Anti-pattern contract** detects template repetition (1000 lines of `console.log("0")`)
- **Pattern contract** (H.2) detects missing abstraction (no loop construct found in the code)
- **Trust scoring** (H.2) penalizes patterns that brute-force their way to passing

## Persona-skills mapping (Phase H.2-bridge)

Each persona contract has `skills.required` (must invoke ≥1) and `skills.recommended` (advisory):

```json
"skills": {
  "required": ["security-audit"],
  "recommended": ["review", "research-mode"]
}
```

Spawn prompts list **skill names only** (not descriptions) — the actor invokes the `Skill` tool to load on demand. Saves ~80% prompt-tokens per skill mention. See pattern: [persona-skills-mapping](patterns/persona-skills-mapping.md).

## Agent identity & reputation (Phase H.2-bridge)

Persona = role; identity = named instance. Each persona has a small roster (e.g. architect → `["mira", "theo", "ari"]`); spawns assign one identity at a time and the identity accumulates per-instance trust across runs. So "I trust mira" becomes meaningful, not just "I trust architects."

```bash
node scripts/agent-team/agent-identity.js init      # one-time, creates ~/.claude/agent-identities.json
node scripts/agent-team/agent-identity.js assign --persona 04-architect    # round-robin returns "mira"
node scripts/agent-team/agent-identity.js stats --identity 04-architect.mira
```

Verifier accepts `--identity persona.name` and `--skills s1,s2`; both flow into `agent-patterns.json` (per-persona aggregate) AND `agent-identities.json` (per-identity track record). See pattern: [agent-identity-reputation](patterns/agent-identity-reputation.md).

## Pattern library

Reusable patterns extracted from HETS development live in `patterns/`. Each pattern has a **summary block (≤5 lines, paste-inline cheap)** and a full doc with intent, components, failure modes, validation strategy. See `patterns/README.md` for the index. Current catalog:

| Pattern | Status |
|---------|--------|
| [Asymmetric Challenger](patterns/asymmetric-challenger.md) | active (shipped H.2.3) |
| [Trust-Tiered Verification Depth](patterns/trust-tiered-verification.md) | active (shipped H.2.4; formula explicit since H.4.2) |
| [Convergence-as-Signal](patterns/convergence-as-signal.md) | observed |
| [Persona-Skills Mapping](patterns/persona-skills-mapping.md) | active (shipped H.2.6) |
| [Agent Identity & Reputation](patterns/agent-identity-reputation.md) | active (shipped H.2-bridge; formula transparency H.4.2) |
| [Meta-Validation](patterns/meta-validation.md) | active |
| [Prompt Distillation](patterns/prompt-distillation.md) | active (continuous practice across spawns) |
| [Shared Knowledge Base](patterns/shared-knowledge-base.md) | active (shipped H.2-bridge.2) |
| [Content-Addressed References](patterns/content-addressed-refs.md) | active (shipped H.2-bridge.2) |
| [Skill Bootstrapping](patterns/skill-bootstrapping.md) | active (shipped H.2.5) |
| [Tech-Stack Analyzer](patterns/tech-stack-analyzer.md) | active (shipped H.2.5) |
| [Structural Code Review](patterns/structural-code-review.md) | active (shipped H.2.7; opted in to 7 builders H.3.4) |
| [KB-Scope Enforcement](patterns/kb-scope-enforcement.md) | active (shipped H.4.0) |

To target a pattern in a future chaos run, read its "Validation Strategy" section — each lists concrete failure modes and how an actor could stress them. `chaos-test --pattern <name>` is planned for full H.2.

## Shared knowledge base (Phase H.2-bridge.2)

`kb/` is the team's shared documentation — one source of truth, content-addressed, snapshot-frozen per run. See [shared-knowledge-base pattern](patterns/shared-knowledge-base.md) and `kb/README.md`.

```bash
# At run start: freeze the manifest into the run-state dir
node scripts/agent-team/kb-resolver.js snapshot ${RUN_ID}

# In spawn prompts: hand actors refs (not inlined content)
# Example skills block: "your KB scope: kb:hets/spawn-conventions@10429c4c"

# Actor's first action: resolve the ref
node scripts/agent-team/kb-resolver.js resolve kb:hets/spawn-conventions@10429c4c
```

Refs of the form `kb:<id>@<short-hash>` validate the doc hasn't drifted since the snapshot. See [content-addressed-refs pattern](patterns/content-addressed-refs.md). Starter KB:
- `kb:hets/spawn-conventions` — the canonical 5-step spawn convention
- `kb:hets/identity-roster` — per-persona identity rosters
- `kb:web-dev/react-essentials` — reference doc for the planned `09-react-frontend` persona

## Files in this skill

- `SKILL.md` — this file
- `contract-format.md` — full spec for contract JSON
- `BACKLOG.md` — deferred work + rationale (added in H.2.1)
- `patterns/` — reusable architectural patterns (substrate for new simulations)
- `kb/` — shared knowledge base (content-addressed, frozen-per-run)
- `role-templates/pm.md` — super-agent role template
- `role-templates/senior.md` — orchestrator role template
- `role-templates/engineer.md` — actor role template

## Files this skill consumes (in scripts/agent-team/)

- `tree-tracker.js` — BFS/DFS over the spawn tree, persisted to tree.json
- `contract-verifier.js` — runs functional + anti-pattern checks (post-fix: prototype-pollution-safe, .every semantics, valid JS regex for end-of-input)
- `pattern-recorder.js` — appends results to ~/.claude/agent-patterns.json; forwards `--identity` to agent-identity.js when supplied
- `agent-identity.js` — assign/list/stats/record per-identity; round-robin assignment with file-locked persistence to ~/.claude/agent-identities.json
- `kb-resolver.js` — content-addressed KB resolver (cat / hash / list / resolve / scan / snapshot / register)
- `budget-tracker.js` (H.2.8) — per-spawn token-usage tracking + on-demand budget extensions; reads contract `budget.{tokens, extensible, maxExtensions, extensionAmount}` and enforces them. Closes the architect's "budget enforcement is fictional" finding from chaos-20260502-060039. Per-run state in `swarm/run-state/<run-id>/budgets.json`.
- `pattern-runner.js` (H.2.9) — extracts testable scenarios from pattern docs' `## Validation Strategy` section; emits actor-prompt skeletons for `chaos-test --pattern <name>` flow. Subcommands: `list-patterns`, `extract`, `summary`, `prompts`.
- `contracts-validate.js` (H.3.0) — cross-validates 4 sources of truth (per-pattern frontmatter ↔ patterns/README.md ↔ SKILL.md catalog ↔ contract `skill_status` filesystem references). 7 validators: pattern-status-frontmatter, pattern-status-readme-consistency, pattern-status-skill-md-consistency, pattern-related-bidirectional, contract-skills-status-keys, contract-skill-status-values, contract-kb-scope-resolves. Closes architect's #1 top-leverage change from chaos-20260502-060039. CS-1 first run surfaced 29 real drift violations; backlog item per validator.
- (Phase H.2: `trust-tracker.js` — persists per-persona trust scores; superseded by `agent-identity.js`'s tier API in H.2.4)

## Phase status

- **H.1 (shipped)**: tree tracking, functional + anti-pattern contracts, self-learning recorder
- **H.2-bridge (this phase)**: verifier-bug fixes (C-1 prototype pollution, H-1 `.some` semantics, `\Z` regex), persona-skills mapping in contracts, identity registry + per-identity recording, patterns library
- **H.2-bridge.2 (shipped)**: shared knowledge base + content-addressed refs (`kb-resolver.js`), 4 new pattern docs (shared-KB, content-addressed-refs, skill-bootstrapping, tech-stack-analyzer), 3 starter KB docs
- **H.2.1 (shipped — vertical slice)**: first builder persona end-to-end. `06-ios-developer` persona + contract with promise-mode skill_status; `swift-development` specialist skill; `kb:mobile-dev/{swift-essentials,ios-app-architecture}` KB docs; identity roster `[riley, morgan, taylor]`. **Bonus fixes bundled**: tree-tracker path resolution (env var override), tree-tracker H-2 cycle guard, tree-tracker M-2 collision warning, verifier `[a-z]{1,4}` → `[a-z]{1,10}` regex (was rejecting `.swift`/`.kotlin`/`.python` — surfaced by the vertical slice itself).
- **H.2.2 (shipped — builder family complete)**: 6 more builder personas: `07-java-backend`, `08-ml-engineer`, `09-react-frontend`, `10-devops-sre`, `11-data-engineer`, `12-security-engineer`. Each with persona + contract + identity roster. 11 starter KB docs across 6 new domains (`backend-dev/`, `ml-dev/`, `web-dev/typescript-react-patterns`, `infra-dev/`, `data-dev/`, `security-dev/`). **Marketplace integration bonus**: contracts reference `knowledge-work-plugins` skills (e.g., `engineering:incident-response`, `data:sql-queries`, `design:accessibility-review`) via new `marketplace:<plugin>` status value alongside `available` and `not-yet-authored`. Spec documented in `contract-format.md`. E2E validated: 6 identities assignable, sample contract verifier pass with marketplace-namespaced skill invocations recorded.
- **H.2.3 (shipped — asymmetric challenger)**: shared `challenger.contract.json` template (~10K token budget, F3=`noEmptyChallengeSection`, A1=`noPaddingPhrases` with stricter list catching capitulation drift). New `agent-identity assign-challenger --exclude-persona X` subcommand prefers different-persona pick (falls back to same-persona-different-identity if no different-persona available). New `kb:hets/challenger-conventions` doc (5-step spawn flow + output format + failure modes). Pattern doc status `proposed → implementing`. E2E validated 4 probes: assign-challenger picks different persona (probe 1+2), GOOD challenger output verifies pass (probe 3), EMPTY challenger fails on F3 (no challenges) AND A1 (padding phrase) — both defenses fire as designed.
- **H.2.4 (shipped — trust-tiered verification, LATENCY-CRITICAL)**: queryable tier API in `agent-identity.js` (`tier --identity X`, `recommend-verification --identity X`); verification policy table (high-trust → spot-check + skip `noTextSimilarityToPriorRun`; medium-trust → asymmetric challenger; low-trust + unproven → symmetric pair). New `--skip-checks <ids-or-names>` flag on `contract-verifier.js` skips listed checks with `status: 'skipped'` audit trail. New `kb:hets/symmetric-pair-conventions` doc. Pattern doc status `proposed → implementing`. New `HETS_IDENTITY_STORE` env var lets ephemeral runs use temp registries (used by E2E fixtures). E2E validated 4 probes against seeded fixture covering all 4 tier levels: tier formula correct; recommend-verification policy correct; --skip-checks works by check.id and by check.check name.
- **H.2.5 (shipped — tech-stack analyzer + skill-bootstrapping wiring)**: new top-level skill `skills/tech-stack-analyzer/SKILL.md` (orchestrator-side entry point for "build me X" tasks; 7-step workflow with 2 user-gates). New `kb:hets/stack-skill-map` (12 stack entries mapping web / mobile / backend / data / ml / infra / security domains → required + recommended skills + suggested personas + rationale). New `/build-team` command as user-facing entry point. Patterns `tech-stack-analyzer` + `skill-bootstrapping` status `proposed → implementing`. E2E validated 6 probes: stack-skill-map resolves via `cat` and via hash-pinned ref; skill scaffold present + correct trigger sections; command exists; cross-validation confirms 7 stack-skill-map skill names map to real persona contracts AND 4 marketplace skills exist on disk in `~/.claude/plugins/marketplaces/knowledge-work-plugins/`.
- **H.2.6 (shipped — invokesRequiredSkills verifier check)**: new functional check that reads the actor's transcript JSONL (`--transcript <path>`) and verifies each `skills.required` was invoked via the `Skill` tool. Falls back to `--skills` CLI flag when no transcript supplied; graceful pass with `reason: 'no_skills_source_supplied'` when neither. Skips skills with `skill_status: 'not-yet-authored'` (promise mode). Functional-check dispatcher extended to support rich `{pass, ...meta}` returns alongside boolean returns (mirrors antiPattern shape). `persona-skills-mapping` pattern status `implementing → active` (now has enforcement teeth). E2E validated 5 probes: transcript-with-required PASS; transcript-empty FAIL on missing skills (promise-mode skipped correctly); --skills fallback PASS; no-source-graceful PASS with reason; missing-transcript-file FAIL with reason.
- **H.2.7 (shipped — full pattern contracts, third-leg of triple defense)**: closes the long-standing DOCUMENTATION-DEBT flag where SKILL.md described "triple contract" but only 2/3 were implemented. New functional checks: `noUnrolledLoops` (scans code blocks for ≥N identical lines = manual unrolling) + `noExcessiveNesting` (brace-counting depth on C-family code blocks; default `maxDepth: 4`, matches CLAUDE.md fundamentals; strips string literals + comments before counting). New pattern doc `patterns/structural-code-review.md`. The 1000-zeros example now trips `noUnrolledLoops` at the 5th repetition — closes the long-running anti-pattern oversell. E2E validated 5 probes covering unrolled-loop detection, excessive-nesting detection, clean-code passes, Python-indentation limitation (documented), and code-block extraction.
- **H.2.8 (shipped — on-demand budget extensions)**: closes the architect's "budget enforcement is fictional" finding from chaos-20260502-060039. New `scripts/agent-team/budget-tracker.js` with 5 subcommands: `init`, `record` (manual), `record-from-transcript` (auto-extracts `usage.input_tokens` / `output_tokens` / `cache_creation_input_tokens` / `cache_read_input_tokens` from JSONL), `extend` (returns approve/deny based on contract policy), `status` (run + per-identity views). Per-run state in `swarm/run-state/<run-id>/budgets.json` (gitignored). Reads contract budget fields (`tokens`, `extensible`, `maxExtensions`, `extensionAmount`) — same fields previously documentation-only. E2E validated 7 probes: init creates file; manual record updates totals + reports remaining; transcript extraction sums all usage subcategories correctly; extend approve increments + extends allowance; extend deny when exhausted; run-level status aggregates across spawns; per-identity status surfaces utilization%. **First phase using the new git workflow** — branched on `feat/phase-H.2.8-budget-extensions`, PR merged to main.
- **H.2.9 (shipped — pattern-targeted chaos test runner)**: new `scripts/agent-team/pattern-runner.js` with 4 subcommands (`list-patterns`, `extract`, `summary`, `prompts`). Reads each pattern doc's `## Validation Strategy` section, extracts bullets as testable scenarios, emits ready-to-paste actor-prompt skeletons. Case-insensitive header match (caught a real bug: `structural-code-review.md` uses lowercase "strategy" — initially extracted 0 scenarios, now extracts 5). `chaos-test` command updated with `--pattern <name>` workflow. Total scenarios across the 12 patterns: 49. **All H.2 items now shipped**.
- **CS-1 meta-validation chaos run (chaos-20260502-115628-cs1-meta)**: 5 auditor personas independently surfaced 54 findings across H.2.x. Diagnosis: "substrate-rich but call-site-poor". 4 CRITICAL bugs (kb-resolver path traversal, budget-tracker concurrency loses 32% tokens, `--skip-checks` defeats trust enforcement, `unknown_check` still passes for required checks). Re-opens triple-contract oversell (H.2.7 shipped checks, ZERO contracts opt in). Resolution score on 5 prior architect findings: 5/20 (25%). Step-function improvement: git workflow adoption 0% → 100% post-CONTRIBUTING.md. Process meta-finding: background-spawn execution didn't survive a session-boundary; 3 of 5 actors had to be re-spawned in foreground.
- **H.3.0 (shipped — contracts-validate.js, the single highest-leverage closure per CS-1 super-root)**: pulled from BACKLOG since chaos-20260502-060039 (and re-flagged by CS-1 architect as still unshipped). 7 validators that cross-check the 4 sources of truth that drift independently. First production run surfaced 29 real drift violations confirming CS-1 architect findings (4 pattern-status-readme drifts, 10 pattern-related asymmetric links, 14 contract-skills-status missing entries in auditor contracts, 1 available-but-missing skill file). Validator is read-only — finding fixes is the next phase. Lint-time drift detection now possible.
- **H.3.1 (shipped — quick-wins bundle)**: 5 fixes closing 4 BACKLOG items + 1 CS-1 architect HIGH. (a) `mustNotSkip` flag on contract checks: `--skip-checks` now refuses to skip checks marked mustNotSkip (closes CS-1 hacker.zoe CRIT-2 "trust enforcement bypassable"). (b) `unknown_check` on required functional check now fails the verdict (closes CS-1 hacker.zoe CRIT-1 + BACKLOG.md item from H.2-bridge). (c) `max_depth` enforcement in `tree-tracker.js spawn` — refuses to spawn beyond depth (default 3); closes the architect's "ZERO progress in 9 sub-phases" finding. (d) `hierarchical-aggregate.js` path resolution standardized to env-var override + canonical fallback (matches H.2.1 tree-tracker fix). (e) `cmdSpawn` self-cycle guard (`--child === --parent` rejected; closes CS-1 code-reviewer C-2). E2E validated 5 probes.
- **H.3.2 (shipped — shared `_lib/lock.js` + path traversal fix)**: extracted shared `withLock` primitive to `scripts/agent-team/_lib/lock.js` (unifies prior inline copies in `agent-identity.js` + `pattern-recorder.js`); applied to 3 more RMW scripts (`kb-resolver register`, `budget-tracker record`, `tree-tracker save`). Stale-lock recovery via PID liveness check. Closes CS-1 hacker.zoe CRIT-4 (budget-tracker concurrency loses 32% tokens) + code-reviewer X-3 (tree-tracker race) via the lock library. **Own-validation probe 3 caught a fresh bug in the H.3.2 fix itself**: first version wrapped only `writeBudgetsAtomic` in withLock (still 23% loss), since concurrent loaders read pre-modification state then serialize their writes. Fix: lock the whole RMW at the callsite (`cmdRecord` wraps load+modify+write in `withBudgetLock`); 0% loss. Also: `kb-resolver` path-traversal fix via `path.resolve` boundary check (closes CS-1 hacker.zoe CRIT-3 + code-reviewer H-4 — `kb-resolver cat ../../etc/secrets` no longer reads outside KB_BASE).
- **H.3.3 (shipped — install.sh sync + post-phase install convention)**: re-ran `install.sh` to bring `~/.claude/scripts/agent-team/` to parity with the toolkit canonical copy after H.2.x → H.3.2 drift. Added "sync after every phase" step to CONTRIBUTING.md.
- **H.3.4 (shipped — structural checks opt-in across 7 builder contracts)**: opted contracts 06–12 (iOS, Java, ML, React, DevOps, data, security) into `noUnrolledLoops` (maxRepetitions: 5) + `noExcessiveNesting` (maxDepth: 4). Closes the H.2.7 oversell (shipped checks but ZERO contracts opted in) flagged by CS-1.
- **H.3.5 (shipped — fix all 29 contracts-validate violations)**: resolved every drift finding from H.3.0's first run. 4 pattern-status README mismatches, 10 asymmetric `Related` links, 14 missing `contract-skills-status` entries in auditor contracts (01-05), 1 missing skill file. `node scripts/agent-team/contracts-validate.js` now reports 0 violations across all 7 validators. Lint-time drift detection achieves green-baseline state.
- **CS-2 second meta-validation chaos run (chaos-20260503-154327-cs2)**: re-ran 5 auditors against H.3.0–H.3.5. Surfaced 5 regressions: (1) `_lib/lock.js:37-40` self-PID deadlock (lock holder's prior incarnation crashed leaves same-PID orphan; existing `pid !== process.pid` check skips cleanup); (2) `tree-tracker.js` H.3.2 fix wrapped only the WRITE in withLock — same RMW-race pattern that own-validation probe 3 explicitly proved fatal in budget-tracker; not back-applied to tree-tracker; (3) `contract-verifier.js:388-391` antiPattern dispatch path silently passes `unknown_check` — H.3.1's symmetric fix to the FUNCTIONAL path was not mirrored; (4) `kb-resolver.js` H.3.2 boundary check is lexical-only — symlinks inside KB_BASE pointing outside still escape; (5) `contracts-validate.js --list-validators` always emits JSON regardless of mode (no human-mode parity). Architect resolution score: 12/20 (60%) — meaningful progress over CS-1's 5/20, unmoved items concentrate at orchestration/runtime/single-source-of-truth layer (e.g., `kb_scope` still unconsumed). Process win: post-CONTRIBUTING.md git workflow held 100% (6/6 H.3.x phases through PR flow with annotated tags, zero direct-to-main).
- **H.3.6 (shipped — CS-2 regression fixes)**: bundle of 5 fixes for the regressions above. (1) `_lib/lock.js`: self-PID orphan now reclaimed (treat as stale; `pid === process.pid` is impossible mid-`withLock` so it must be a prior-incarnation orphan); also handles garbage-PID via NaN guard. (2) `tree-tracker.js`: `cmdSpawn` + `cmdComplete` wrap their entire load→modify→save cycle in `withTreeLock` (mirrors budget-tracker `cmdRecord` pattern); 15s timeout for chaos-test contention. `save()` split into plain `writeTreeAtomic` (no lock) + `withTreeLock` helper. (3) `contract-verifier.js`: antiPattern `unknown_check` dispatch now increments failures (`severity === 'fail'` → `antiPatternFailures`; else → `antiPatternWarns`) — symmetric with H.3.1 functional path. (4) `kb-resolver.js`: lexical check kept as cheap first-pass (catches `..`-traversal on missing files); added `fs.realpathSync` second-pass that canonicalizes both candidate + KB_BASE then re-checks boundary — symlinks pointing outside refused. (5) `contracts-validate.js --list-validators`: respects `--json` flag (default human-readable; emits `Available validators (N): ...` listing). E2E validated all 5 probes + kb-resolver legit-read regression check passed. **Pattern observed across H.3.x cycle**: own-validation routinely catches fix-class-not-instance bugs (the bug your fix shipped with). Codify "fix-class-not-instance — when fixing a known anti-pattern, grep sibling files for the same shape" — H.3.6 itself was triggered by CS-2 catching that H.3.2's RMW-race fix wasn't back-applied to tree-tracker.
- **H.4.0 (shipped — kb_scope enforcement, top architect-unmoved item)**: closes the #1 unmoved finding from CS-1+CS-2 architects: `kb_scope` was loaded into spawn-time prompts but never enforced at verify-time. New `kb_scope_consumed` functional check + `extractKbReadsFromTranscript` helper in `contract-verifier.js`. Detects three KB-read invocation shapes from JSONL transcripts: Bash `kb-resolver(.js)? cat <id>` (bare or `kb:`-prefixed), Bash `kb-resolver(.js)? resolve kb:<id>[@<short-hash>]`, and `Read` against `skills/agent-team/kb/<domain>/<doc>.md`. Hash-pinned refs strip `@hash` before matching. Rich result: `{pass, source, declared, consumed, kbReadsObserved, missingKbScope}`. Graceful pass when no transcript supplied (mirrors H.2.6 `invokesRequiredSkills` semantics) so spawn-time invocations remain backwards-compatible; enforcement triggers only when a transcript is fed in at verification time. **No `--kb-reads` CLI fallback** — would defeat the anti-self-reporting-drift goal. Opted 8 contracts into the check at `required: true`: 7 builders (06-12) get F9, challenger gets F6. New pattern doc `patterns/kb-scope-enforcement.md` (status `active` — same tier as `persona-skills-mapping` since it's the same enforcement-with-teeth shape). E2E validated 5 probes covering full-pass, missing-kb, no-scope-declared, no-transcript, invalid-transcript. contracts-validate: 0 violations after bidirectional back-links added to 3 related patterns. **Architect-resolution movement**: CS-2 unmoved item "kb_scope is loaded but never enforced" — now closed. Same template applies to other "declared but unenforced" contract fields.
- **H.4.1 (shipped — auto self-improve loop)**: closes the user concern that `/self-improve` required manual invocation, which limited self-learning to user intervention. New `scripts/self-improve-store.js` library (8 CLI subcommands: bump, bump-turn, scan, pending, dismiss, promote, reset, stats) backs a 4-trigger architecture: Stop hook (every turn) bumps per-signal counters; Stop hook (every 30th turn) triggers consolidation scan; PreCompact hook triggers consolidation scan; new UserPromptSubmit hook (`session-self-improve-prompt.js`) injects ONE batched approval reminder on first prompt of session, idempotent within session via `lastShownInSessionId`. **Threshold-based promotion** (mirrors prompt-pattern-store's 5+-approval auto-apply): ≥5 occurrences → queued candidate; ≥10 occurrences AND `risk='low'` → auto-graduated to `~/.claude/checkpoints/observations.log`. Risk taxonomy: `observation-log`/`memory-consolidation` (low, auto), `skill-candidate` (medium, prompt), `rule-candidate`/`agent-evolution` (high, always prompt — Memory→Rule stays manual via `/self-improve`). State at `~/.claude/self-improve-counters.json` + `~/.claude/checkpoints/self-improve-pending.json`. E2E validated 35-turn replay (4 signals captured, scan fires at turn 30, 2 auto-graduated + 2 pending), session-scoped idempotency, queue-empty silence. README disclaimer #3 evolved from ❌ "no auto-promotion" to ⚠️ "partial auto-promotion — load-bearing promotions still need /self-improve."
- **H.4.2 (shipped — validator-subdir hooks + trust-formula transparency)**: bundle of 2 hook-layer enforcement additions + 1 doc clarification deferred from H.4.1 scope. (a) `hooks/scripts/validators/validate-no-bare-secrets.js` — PreToolUse:Edit/Write blocks writes containing secret-shaped literals (Anthropic/Stripe/Slack/GitHub/AWS/JWT + generic `*_KEY=value` assignments). Placeholder values (`<YOUR_KEY>`, `${VAR}`, `aaaaaaaa…`) are skipped; `.env.example`/test-fixture paths are in the skip list. **Never echoes the matched literal in block reasons** — log files / chat transcripts don't preserve exposed secrets. Closes the security.md pre-commit checklist's "no hardcoded secrets" item at the hook layer (deterministic), complementing the actor-output-only triple-contract (post-hoc). (b) `hooks/scripts/validators/validate-frontmatter-on-skills.js` — PreToolUse:Write blocks skill / pattern .md files that lack YAML frontmatter (silently-broken skills are worse than visibly-broken ones). README/BACKLOG/CHANGELOG basenames excluded. (c) `patterns/agent-identity-reputation.md` adds a new "Trust Formula" section that documents the actual `tierOf()` implementation from `agent-identity.js:97-104` (passRate-with-min-runs gate), worked examples, the tier-to-policy mapping table, and known limitations (no recency decay, partial=miss, cliff thresholds). Closes the ruflo comparison's "their formula is published, ours is opaque" gap. E2E validated 8 probes (4 secrets, 4 frontmatter) covering block + approve + skip-list paths. **Manual settings.json merge** still required (config-guard correctly blocks direct edits — propose via `update-config` skill going forward).
- **H.5.0 (shipped — official Claude Code plugin packaging)**: repackaged the toolkit as an installable Claude Code plugin per the official `code.claude.com/docs/en/plugins-reference` schema. Three new manifests at repo root: (a) `.claude-plugin/plugin.json` — name, version (0.5.0), description, author, repo, license (MIT), 13 keywords. Description tightens positioning: "Deterministic operating substrate for Claude Code — hooks before, persistence around, verification after — compensates for LLM non-determinism at the seams without trying to replace the LLM." (b) `hooks/hooks.json` — mirrors `~/.claude/settings.json` structure, 11 hook entries across 5 lifecycle events (SessionStart, UserPromptSubmit×2, PreToolUse×4, PreCompact, Stop×3), uses `${CLAUDE_PLUGIN_ROOT}` substitution so paths resolve regardless of install location. (c) `marketplace.json` — single-plugin marketplace at repo root, lets users `/plugin marketplace add shashankcm95/claude-skills-consolidated` then `/plugin install claude-skills-consolidated`. README rewritten with anti-AI-slop differentiation table comparing this plugin to typical SKILL.md prompt templates: 11 hooks vs 0–2, multi-agent coordination vs single-agent, triple-contract verification vs none, persistence across sessions vs stateless, threshold-based auto self-improve vs static, chaos-test meta-validation vs none, 13 documented patterns vs implicit, contracts-validate auditability vs vibes. Two install paths supported (plugin marketplace + legacy `install.sh`); existing users unaffected.
- **CS-3 Track 1 — programmatic coverage audit (no LLM)**: deterministic dead-code + pattern-invocation analysis across the post-H.5.0 toolkit. Findings: hooks 11/11 wired (100%), substrate scripts all referenced ≥3 times (no dead code in `scripts/agent-team/`), self-improve loop confirmed live (counters bumping per turn), 9 of 13 patterns stuck at `status: implementing` despite shipping phases ago (status drift), 3 KB docs not in any contract `kb_scope.default` (turned out to be intentionally exempt — toolkit-meta orchestration docs vs persona-domain knowledge), all 7 builder personas (06-12) at `unproven` tier with only assign-test spawns recorded (genuine coverage gap — the substrate works but has no real-task usage data). CS-2 architect's "substrate-rich, call-site-poor" finding manifested again: 12 of 13 patterns have 0 contract refs (only `asymmetric-challenger` is contract-enforced). H.5.1 fixes the cosmetic drift; deeper pattern→enforcement bridge (H.5.3) deferred to its own phase.
- **CS-3 Track 2 chaos run (chaos-20260505-095622-cs3)**: 5-auditor swarm against post-CS-2 infrastructure (H.3.6 → H.5.1). **Architect resolution score 14/20 (+2 from CS-2's 12/20)** — driven entirely by H.4.1 closing the self-learning loop trigger gap. Honesty-audit headline: 14 VERIFIED, 4 PARTIAL, 0 UNSUPPORTED, 0 WRONG of 18 claims tested (vs CS-2's ~17% commit-message oversell — major doc-accuracy improvement). Trust-formula doc cited as "gold standard for documentation honesty." 5 new CRITICAL findings: (1) `kb_scope_consumed` is text-pattern not provenance — actor can spoof via `echo "kb-resolver cat foo"`; (2) secrets validator misses `github_pat_` (modern PAT format) AND fails open on parse error AND self-skips its own dir; (3) README hook count contradicts itself 5+ places (says "11" + "(6)" + "6 hook scripts"); (4) README has TWO `## Install` sections, plugin route doesn't mention settings.json merge step; (5) Edit-tool secret scan reads non-existent field `replace_all_string`. CS-2 unmoved roll-up: 2 of 8 RESOLVED (kb_scope substrate, antiPattern symmetry); 6 still UNMOVED including `hierarchical-aggregate.js` location (5 consecutive runs). Process meta-finding: 3 of 5 background spawns lost (regression to CS-1 pattern; CS-2's foreground 5/5 was the better baseline). Pattern→enforcement bridge persists: 12 of 13 patterns 0 contract refs (3rd consecutive architect finding).
- **H.6.9 (shipped — full post-H.6.7 orchestration test cycle; 5/5 PASS)**: closes the original H.6.1 5-task plan end-to-end. Tasks 2-5 (React pagination, k8s manifest, OAuth audit, ETL pipeline) all PASSed atop the H.6.8 closure of Task 1 (Express rate-limiting). **Cycle headline**: 5 PASSes, 46 findings, 120 file:line citations across 347K tokens of agent work + ~25K tokens of root-side skill authoring. **Skills forged this cycle from canonical sources**: `react` (react.dev/reference/react), `kubernetes` (kubernetes.io/docs/home/), `penetration-testing` (owasp.org/www-project-web-security-testing-guide/), `airflow` (airflow.apache.org/docs/) — `airflow` was NOT in the H.6.7 starter registry; niko (Task 5) emitted an `extend-canonical-sources` request via H.6.5 missing-capability-signal pattern; root acted on it this cycle (registry now 24 entries). **5 builder personas now have first verdicts**: 13-node-backend.kira (medium-trust, 0.667 passRate), 09-react-frontend.casey (passRate=1.0 but unproven at 3 verdicts < 5-threshold), 10-devops-sre.hugo / 12-security-engineer.vlad / 11-data-engineer.niko (each first verdict). Combined with mio's H.5.6 verdict, toolkit now has **6 builder identities with real trust-formula data** — 30% of the way to H.7.0's ≥20-verdict L3-design threshold. **6 meta-findings surfaced**: (M-1) severity-section finding placement is the convention, generic `## Findings` is the trap — propagated to Tasks 2-5 successfully; (M-2) cwd-relative paths fragile across spawn sessions — Task 4 dropped at portfolio-builder cwd, manual copy needed; explicit absolute-path instruction in Task 5 fixed it; (M-3) root-authoring-skills is structurally cheaper than sub-agent forge (~25K root vs ~125-175K sub-agent across 5 forges); (M-4) substrate audit BEFORE spawn caught zero issues post-H.6.7 — H.6.2-H.6.7 substrate fixes are durable; (M-5) contract-shape mismatch (H.5.7 backlog) was real for 4/5 builder tasks — engineering work contorted into audit-shape; verdicts genuine but friction is real, H.5.7 promoted to higher priority; (M-6) foreground spawning held 5/5 — CS-1+CS-3's background-spawn loss pattern fully avoided. **Capability gaps surfaced**: 8 H.6.5 missing-capability-signal returns from sub-agents — 1 acted on (airflow registry extension), 7 logged to backlog (forge-skill express, postgres-engineering, react-testing, external-secrets-operator; author-kb-doc redis-pool-patterns, accessibility-pagination, prometheus-patterns). Aggregate findings doc at `swarm/H.6.9-orchestration-cycle-findings.md` (parallel to H.6.1-orchestration-test-findings.md + H.6.1-resume-findings.md). The H.6.x cycle (H.6.0 spawn-recorder → H.6.9 cycle aggregation) is now CLOSED.
- **H.6.8 (shipped — first post-H.6.7 orchestration test; H.6.1 closure)**: re-ran the H.6.1 task (*"Add rate limiting to my Express API endpoints"*) end-to-end after the H.6.2-H.6.7 substrate fixes. **Substrate audit** (deterministic, no LLM): tech-stack-analyzer routes Node/Express → 13-node-backend (H.6.2 ✓); `agent-identity assign` returns `forgeNeeded.required: [node-backend-development]` (H.6.3 ✓); root forges `node-backend-development` skill from `nodejs.org/docs/latest/api/` per H.6.7 canonical-source registry (root authoring per H.6.5 convention — no sub-agent self-authoring); contract `skill_status` updated to `available`; re-verify shows `forgeNeeded.required: []` (substrate green). **Spawn**: foreground Agent invocation as `13-node-backend.kira` with full contract + KB scope + 35K-token budget. Agent authored 3 implementation files (rate-limiter middleware 175 LoC, example app 130 LoC, `node:test` integration suite 228 LoC — all pass `node --check`) + findings report at `swarm/run-state/orch-test-rate-limiting-resume-20260506-123901/node-actor-13-node-backend-kira.md`. **Independent contract-verifier verdict**: `pass` across all 9 functional + 5 antiPattern checks (functionalFailures: 0, antiPatternFailures: 0, findingsCount: 6, fileCitations: 33, recommendation: `accept`). **Trust signal**: kira moves from `passRate=0.6` → `passRate=0.667`; tier stays `medium-trust` (high-trust threshold is 0.8). **Capability gaps surfaced** (H.6.5 missing-capability-signal returns from agent's `## Notes`): 3 structured requests — forge-skill `express`, forge-skill `postgres-engineering`, author-kb-doc `backend-dev/redis-pool-patterns`. All deferred (KB sufficient for current task scope; revisit when first task surfaces hard need). **Meta-finding**: agent's first-pass output had findings under generic `## Findings` heading; verifier's `minFindings` regex counts entries *under severity sections* specifically — not `## Findings`. Agent self-corrected to bolded bullets under `## HIGH` / `## MEDIUM` / `## LOW`. Logged as small-fix BACKLOG: update `kb:hets/spawn-conventions` to be explicit about WHERE findings get counted. **Significance**: this is the **first 13-node-backend verdict ever recorded against a real engineering task** — combined with mio's H.5.6 verdict (12-security-engineer's first PASS), the toolkit now has 2 builder personas with real trust-formula data populated. Closes the H.6.1 abort end-to-end (the task that triggered the H.6.x cycle); validates that the post-H.6.7 substrate permits coherent build-team orchestration for any Node/Express task. Findings doc at `swarm/H.6.1-resume-findings.md`. Tasks 2-5 of the original H.6.1 plan (React form, k8s manifest, OAuth audit, ETL pipeline) queued next as separate orchestration runs.
- **H.6.7 (shipped — canonical-source registry; L2 of evolution loop)**: closes the user's directive *"the skill-forge should not only scour the internet for skill gaining, but also include official documentation wherever prudent — especially for tech skills that usually have an official page with all the syntax and patterns."* New KB doc `kb:hets/canonical-skill-sources` (23 entries across 6 domains: web/frontend, backend, mobile, data/ML, infra/devops, security) maps skill names to authoritative documentation URLs (e.g., `react → react.dev/reference`, `kubernetes → kubernetes.io/docs/home/`, `spring-boot → docs.spring.io/spring-boot/index.html`, `swift-development → swift.org/documentation/`). Each entry has a `type` field (`reference` / `book` / `getting-started` / `spec`) to scope research depth + a `notes` field for version pinning + framing nuances (e.g., "App Router post-13.4 unless task says Pages Router"). Skill-forge skill (`skills/skill-forge/SKILL.md`) updated with new step 2a (canonical-source lookup) — consults the registry FIRST; falls back to generic internet research only when no canonical source exists (no regression). Pattern doc `patterns/skill-bootstrapping.md` updated to reference the registry + acknowledge the failure modes it reduces (low-quality bootstrap + licensing risk). New failure mode #5 acknowledged: stale URLs (counter: quarterly audit + version-pinning in `notes` field). **Why canonical-first matters**: a skill forged from `react.dev/reference` encodes the React team's idioms; one forged from "react best practices 2024" encodes whichever blog ranked highest. Better INPUTS to the substrate produce higher-quality forged skills, faster trust accumulation, faster L3 selection signal. **L3 (evolution loop / breeding) remains deferred** to H.7.0 — no rules to design without ≥20 verdicts. KB doc registered + resolves cleanly via `kb:hets/canonical-skill-sources@106baa33`. Closes the second of the three "yes to all three" items from the user's H.6.6 confirmation message.
- **H.6.6 (shipped — lifecycle primitives + L3-forward schema)**: closes the user's chicken-breeding-analogy vision (selection → reproduction → culling → generational specialization) by shipping **L1: lifecycle primitives** as the substrate evolution will run on. Two new `agent-identity` subcommands: (a) `prune` walks the identity store and emits structured recommendations — **retire** (when `verdicts ≥ 10` AND `passRate < 0.3` — soft-retire, audit trail preserved) + **tag-specialist** (when `verdicts ≥ 5` AND `passRate ≥ 0.8` AND a single `skillInvocations[X] ≥ 3` — adds the skill to `specializations[]` + populates `traits.skillFocus`). Advisory-by-default; `--auto` flag applies. CLI threshold tunables (`--retire-min-verdicts`, `--retire-pass-rate-max`, `--specialist-min-verdicts`, `--specialist-pass-rate-min`, `--specialist-min-invocations`). (b) `unretire --identity X` restores a soft-retired identity (mistake-recovery, reversible by design). `cmdAssign` updated to filter retired identities from the round-robin (still in JSON for replay; just skipped at assign-time). **Schema additions, forward-compatible for H.7.0** (the deferred breeding loop): every identity now carries `retired: bool, retiredAt: ISO, retiredReason: string, parent: identity-id | null, generation: int, traits: { skillFocus, kbFocus, taskDomain }`. Today only `retired*` + `traits.skillFocus` are populated (by `prune`); `parent` is null and `generation` is 0 for all round-robin originals. The schema lands now so when L3 (breeding) lands, prior identity data is already L3-shaped — no migration needed. **`_backfillH66Schema` helper** brings legacy records (pre-H.6.6) up to the new shape on read. **L2 (canonical-source registry — H.6.7)** ships separately: extends skill-forge to consult an authoritative-sources registry before generic internet research. **L3 (evolution loop — H.7.0)** deferred until ≥20 real builder verdicts accumulate; today's n=1 makes empirical breeding-rule design impossible. Pattern doc `agent-identity-reputation.md` extended with new "Lifecycle + Evolution Vision (H.6.6 + H.7.0)" section explaining the staged rollout, schema additions, prune rules, and pre-L3 design constraints. E2E validated 5 probes: advisory prune (no mutation), retire-candidate triggered correctly, `--auto` applies retire+specialist, retired identity skipped in assign round-robin, unretire restores. **Convention surfaced**: this is the second time a "convention not new code" approach has worked (first was H.6.5 missing-capability-signal); the toolkit deliberately avoids implementing automation until the data exists to design it correctly.
- **H.6.5 (shipped — missing-capability-signal pattern; autonomous platform extension)**: closes the meta-finding the user surfaced post-H.6.4 — *"the orchestrator/architect is supposed to create personas, not me hand-authoring them."* The toolkit's autonomous-platform goal requires the orchestrator to grow its own substrate (personas, skills, KB docs, stack-map entries) on demand. Until H.6.5, only **skill-forge** existed as an automated authoring path — the other artifacts (persona/contract/KB/stack-map) were entirely manual. H.6.5 closes the gap with a **convention, not new code**: sub-agents (architect, builders, auditors) **diagnose substrate gaps**; root (chat orchestrator) **acquires** them using its full toolkit context. Sub-agents emit a structured `request: {type: forge-persona | forge-skill | author-kb-doc | extend-stack-map, scope, proposed_name, rationale, ...}` in their `## Notes` section; root reads the request, surfaces it to user-gate, then authors files via Edit/Write. **Why this is cheaper than full automation**: file authoring at root is ~0 marginal LLM cost (just tool calls); at sub-agent it's ~25-35K tokens per artifact AND lower-quality (sub-agent has narrow context). Sub-agents are 5K-token *diagnosis* artifacts, not file-authoring engines. New pattern doc `patterns/missing-capability-signal.md` (status: active) formalizes the rule + schema + failure modes + user-gate flow. Updated: 04-architect persona explicitly told "diagnose, don't author"; `kb:hets/spawn-conventions` extended with the capability-gap schema; `commands/build-team.md` extended with step 4 (handle capability requests from sub-agents). **Coverage strategy**: rather than editing 12 persona files (DRY violation), the convention lives in `kb:hets/spawn-conventions` which every persona reads via `kb_scope.default` — `kb_scope_consumed` enforcement makes this contract-binding. Bidirectional back-links added to 3 related patterns (skill-bootstrapping, tech-stack-analyzer, persona-skills-mapping). contracts-validate clean. **The deeper framing**: H.6.5 is the toolkit's commitment to autonomous-platform-not-just-marketplace-plugin — it can grow its own substrate on demand, gated by user approval, without requiring hand-authoring expertise from any user.
- **H.6.3 (shipped — skill-forge auto-warn at assign-time)**: closes the last H.6.1 gap (the only one not addressed by H.6.2 + H.6.4). `agent-identity assign` now reads the persona contract at assign-time and surfaces any `not-yet-authored` skills in a structured `forgeNeeded` field on the output JSON — split into `required` (blockers) and `recommended` (advisory). When `forgeNeeded.required` is non-empty, the output includes a human-readable `warning` field. Optional `--require-forged` flag exits with code 2 instead of warning — used by build-team pipelines that want to hard-block until forge happens. Verified on 13-node-backend (1 required + 4 recommended surfaced) and 12-security-engineer (penetration-testing required surfaced — re-confirms mio's H.5.6 finding now visible at assign-time, not post-hoc). Pattern doc `skills/agent-team/patterns/skill-bootstrapping.md` extended with the explicit 3-step orchestrator flow (assign → forge? → spawn). All 5 H.6.1 gaps are now closed across H.6.2 + H.6.3 + H.6.4 (3 phases, 2 PRs).
- **H.6.2 + H.6.4 (shipped — Node/Express routing coherence)**: closes the load-bearing gap surfaced in H.6.1 (Express tasks couldn't be coherently routed across stack-map / persona / contract / KB layers). **H.6.4** authors a new builder persona `13-node-backend` with Node-specific contract + identity roster `[noor, evan, kira]` + KB scope `[kb:backend-dev/node-runtime-basics, kb:backend-dev/express-essentials, kb:hets/spawn-conventions]`. New persona file (`swarm/personas/13-node-backend.md`, ~85 lines) follows the same shape as 06-ios-developer (template builder persona) — async-first idioms, event-loop awareness, observability discipline. New contract has 9 functional checks + 5 antiPattern checks + opted in to `kb_scope_consumed` (F9). **H.6.2** extends `kb:hets/stack-skill-map` with a "Backend — Node / Express / NestJS service" entry pointing at `[13-node-backend]` with Node-specific skill list; also adds a forward-looking BACKLOG note on Backend — Python (warrants its own 14-python-backend persona). **2 new KB docs**: `kb:backend-dev/node-runtime-basics` (V8 + libuv + event loop + async patterns + module systems + observability) and `kb:backend-dev/express-essentials` (middleware order + input validation + rate limiting + production checklist + common pitfalls). **Re-validated** by re-running the H.6.1 task-1 walkthrough: `agent-identity assign --persona 13-node-backend` returns `13-node-backend.noor`; KB scope resolves; contract is coherent. contracts-validate clean across all 7 validators. The remaining gap from H.6.1 (skill-forge auto-orchestration) is its own phase H.6.3, queued next.
- **H.6.1 (shipped — first abstract-task orchestration walkthrough)**: ran the documented `/build-team` flow on a real abstract task ("Add rate limiting to my Express API endpoints") with no pre-decided persona, capturing every step + gap into `spawn-recorder`. **Spawn aborted** at Phase 2 (closest-fit persona contract check) because routing failed coherently across 4 substrate layers (stack-skill-map, persona contract, KB scope, persona name) — Express/Node.js doesn't appear in the 12 documented stacks; closest match `07-java-backend` has JVM-specific required skill (`spring-boot` not-yet-authored), JVM-only kb_scope (`backend-dev/spring-boot-essentials + jvm-runtime-basics`), and a persona name that explicitly contradicts non-JVM backend work. **7 distinct gaps surfaced before any LLM spawn** — the deterministic substrate audit caught problems that spawning would have obscured under a contorted verdict. Honest orchestrator move: don't spawn against a mismatched contract (would pollute trust-formula data for `07-java-backend` against an impossible task). Findings tracked at `swarm/H.6.1-orchestration-test-findings.md`. Promoted **4 follow-up phases**: H.6.2 (extend stack-skill-map with Node/Express + Go + Rust), H.6.3 (auto-trigger skill-forge from `agent-identity assign`), H.6.4 (architectural decision on 13-node-backend persona vs 07-java-backend rename). 3 of the 5 originally-planned H.6.x tasks would hit similar routing problems → the planned test plan needs H.6.2-H.6.4 to land first. **Meta-finding**: the manual orchestration walkthrough discipline (do the routing analysis BEFORE spawning; capture gaps via spawn-recorder; only spawn when contract fits) is reusable beyond H.6.x audit phase.
- **H.6.0 (shipped — spawn-recorder for orchestration-test visibility)**: new `scripts/agent-team/spawn-recorder.js` (~250 LoC) provides chronological audit logging of every persona spawn + skill resolution + verdict event during orchestration test runs. Complements (does not replace) the existing per-persona `agent-patterns.json` aggregate stats and per-identity `agent-identities.json` track record. Why this exists: those answer "what's persona X's pass rate?" and "what does identity Y look like now?"; spawn-recorder answers "what HAPPENED in run Z?" — the moment-by-moment audit log: which persona was chosen, which skills resolved (or failed to forge), what verdict came back, what gaps were observed. Storage: append-only JSONL at `~/.claude/spawn-history.jsonl`. Schema flexible (extras JSON passthrough); 6 subcommands (`record`, `summary`, `list`, `gaps`, `stats`, `reset`). The `gaps` aggregator is the load-bearing one for H.6.x — surfaces patterns across multiple orchestration tests (e.g., "skill-forge not auto-triggered" appearing 4× of 5 runs is signal). E2E validated 7 probes covering CLI-flag record, stdin-JSON record, summary readout, list, gaps aggregation, stats, and reset. Foundational for H.6.1+ (real-task orchestration tests where we observe what gets invoked vs what we expected).
- **H.5.6 (shipped — first builder dogfood run)**: closes CS-3 architect-theo's last architectural HIGH ("builders 06-12 at unproven tier with no real verdicts after 5 phases of substrate work"). Spawned `12-security-engineer.mio` (fresh identity, totalSpawns=1) on a real engineering task: author `kb_scope.default` + `kb_scope_consumed` opt-in for the 5 auditor contracts (01-05) that H.4.0 left untouched. **Verdict: PASS** across all 9 functional + 5 antiPattern checks. mio's identity record updated: `passRate: 1.0, verdicts.pass: 1, skillInvocations.security-audit: 1`. **First builder verdict ever recorded** — populates real trust-formula data. Mio chose `kb:security-dev/threat-modeling-essentials + kb:hets/spawn-conventions` for 01-hacker (STRIDE framing for adversarial vuln-hunting); `kb:hets/spawn-conventions` only for 02-05 (auditors are deliberately stack-agnostic). contracts-validate: 0 violations after edits. **Mio surfaced a genuine H-1 architectural finding during the run**: the 12-security-engineer contract is shaped for audit-report output (CRITICAL/HIGH/MEDIUM/LOW + minFindings + threat keyword + audit-domain kb_scope), NOT engineering-task output — every builder-dogfood will hit the same shape-mismatch and produce contorted output to satisfy mismatched checks. Logged as H.5.7 candidate (separate `builder-engineering-task.contract.json` template OR conditional-checks-by-task-type). Process win: foreground spawn (single agent) survived cleanly (vs CS-3's 2-of-5 background spawn loss). Architect-resolution-score-relevant: builder-dogfood gap is now closed; CS-1/CS-2/CS-3 architect's full HIGH list is now resolved.
- **H.5.5 (shipped — architectural cleanup)**: closes 2 of theo's 3 architectural HIGHs from CS-3 in one PR. (a) **`hierarchical-aggregate.js` location DECIDED**: 5 consecutive chaos runs flagged the location; H.5.5 commits to the architecturally-correct decision instead of moving. The aggregator stays at `swarm/` because it's chaos-test runtime tooling, not HETS substrate — it operates over `swarm/run-state/<run-id>/node-*.md` files produced by chaos-test runs and is invoked only from chaos-test workflows (`super-agent.md`, `orchestrator.md`, `commands/chaos-test.md`). Substrate scripts at `scripts/agent-team/` are consumed by spawned actors + verifier callsites; the aggregator is consumed only by chaos-test orchestration. SKILL.md:122 reframed from "persistent BACKLOG item to relocate" to "DECIDED — location is correct; framing was wrong." (b) **New `scripts/agent-team/_lib/runState.js`**: closes CS-2 architect-mira's "_lib/ is a directory of one" finding (carried through CS-3 architect-theo without movement). The `RUN_STATE_BASE` constant + `runStateDir(runId)` helper now single-sourced; previously duplicated verbatim in tree-tracker.js, budget-tracker.js, and kb-resolver.js. `_lib/` now has 2 modules (`lock.js` + `runState.js`) — coherent abstraction validated. Env override `HETS_RUN_STATE_DIR` still respected. E2E validated 5 probes (3 substrate scripts work via shared module + env override + file landing locations). The third architectural HIGH (builder dogfood) deferred to H.5.6 since it requires real engineering work, not refactor.
- **H.5.4 (shipped — remaining CS-3 HIGH cluster)**: closes 4 more CS-3 HIGH findings (filePath regex robustness, `${CLAUDE_PLUGIN_ROOT}` verification, README marketplace + install.sh framing). (a) **Shared filePath extractor** at `hooks/scripts/_lib/file-path-pattern.js` (NEW): de-duped from auto-store-enrichment.js + pre-compact-save.js (silent drift between copies on any future change); now handles Unix paths, Windows paths (drive-letter + backslash), and quoted paths-with-spaces. Closes blair H-4. (b) **`${CLAUDE_PLUGIN_ROOT}` verification probe** in `session-reset.js`: detects "plugin manifest exists but isn't being loaded" failure mode by checking if the env var is set + expanded when scripts are running from a plugin install location; emits stderr warning with diagnostic before any security hook silently no-ops. Closes kai H-4. (c) **README marketplace.json layout note**: explicit callout that `.claude-plugin/plugin.json` lives in conventional `.claude-plugin/` dir while `marketplace.json` lives at repo root (so `/plugin marketplace add owner/repo` finds it via `source: "."`); both required, just in different locations. Closes rafael HIGH-1. (d) **README install.sh status clarity**: removed "fallback for manual setups" + "legacy" framings; replaced with "kept indefinitely as fallback for environments without `/plugin marketplace add` support" + explicit note that both paths produce equivalent on-disk state and `install.sh` is **not** deprecated. Closes rafael HIGH-2. E2E validated 5 probes (Unix + Windows + quoted-paths + version-string-rejection + hook-runs-clean). contracts-validate: 0 violations.
- **H.5.3 (shipped — self-improve-store hardening + frontmatter BOM)**: closes 5 CS-3 HIGH findings clustered in `scripts/self-improve-store.js` + 1 in the frontmatter validator. (a) **`writeAtomic` tmp-suffix collision** (kai LOW-1 + blair H-1): tmp suffix was `.tmp.<pid>` — concurrent same-PID writers collided. Now: `pid + hrtime.bigint() + crypto.randomBytes(6).toString('hex')` — 100/100 unique in stress test. (b) **Lock-fallback silent degradation** (kai H-1 + blair H-2): when `_lib/lock.js` is unreachable, `withLock` became a no-op WITHOUT any log. Operators had no visibility. Now: stderr warning on first fallback (idempotent via `_lockFallbackWarned` flag); explicit message tells the operator to install/symlink the agent-team scripts. (c) **Counter+pending corruption silently zeros history** (kai H-1 + blair H-3): `loadCounters` / `loadPending` caught parse errors and returned fresh defaults — the next `writeAtomic` then overwrote the bad file with empty state, permanently destroying forensics. Now: rename the bad file to `<path>.corrupt-<ISO>` BEFORE returning defaults. ENOENT (first-run / post-reset) bypasses quarantine. Verified: BAD JSON → quarantine file exists, original deleted. (d) **`executeGraduation` append race** (kai H-2): observations.log writes used `appendFileSync` without a lock — lines longer than PIPE_BUF (512 bytes Darwin, 4096 Linux) lose atomicity → concurrent writers interleave bytes mid-line → audit log corruption. Now: `withLock(OBSERVATIONS_LOG + '.lock')` around the append + truncate-with-marker for lines > 256 bytes (safely under Darwin's PIPE_BUF). (e) **Frontmatter validator trips on UTF-8 BOM** (blair H-5): Notepad on Windows + some CI tooling inject BOM (`﻿`) on save; the validator saw `<BOM>---\n` and rejected the frontmatter as missing → blocked valid skills with no diagnostic. Now: strip leading BOM before frontmatter check. Verified: BOM-prefixed valid frontmatter → approve; non-frontmatter content → still blocks. contracts-validate: 0 violations after sync.
- **H.5.2 (shipped — CS-3 CRIT bundle)**: closes all 5 CS-3 CRITICALs in one phase. (a) **`kb_scope_consumed` provenance hardening** at `contract-verifier.js:109-180`: 2-pass scan now indexes `tool_result` blocks by `tool_use_id`; only credits KB reads where `tool_result.is_error !== true` AND content is non-empty AND content doesn't match error-shape (`Not found:`, `Error:`, `MODULE_NOT_FOUND`); rejects Bash commands containing shell-evasion shapes (`echo`, `printf`, `cat <<`, leading `#`, comment-piped `| cat`); rejects path-traversal in captured ids (`..`, leading `/`). The H.4.0 closure goal "prevent self-reporting drift" is now actually defended: an actor emitting `echo "kb-resolver cat foo"` fails verification (probe verified). (b) **Secrets validator hardening** at `validate-no-bare-secrets.js`: added `github_pat_<82chars>` regex (modern fine-grained PAT format since 2022, missing from prior shipping); changed `catch` block from `decision: 'approve'` to `decision: 'block'` with INTERNAL ERROR reason (fail-CLOSED on parse error per security-gate principle; fact-force-gate's graceful-approve is correct for discipline checks but wrong for security gates); tightened SKIP_PATH_PATTERNS to narrow `validators/` exclusion to `validators/fixtures/` + `*.test|spec|fixture.*` files (was self-permissive of any write under `validators/`); fixed Edit-tool field reference (`replace_all_string` doesn't exist in Edit's payload — was effectively only scanning `new_string`); added multi-edit fallback (`toolInput.edits[]`) for future Edit shape variants; added NotebookEdit pessimistic scan. (c) **README hook count consistency**: 5 sections updated to `11 hooks` from inconsistent mix of `11`/`(6)`/`6 hook scripts` — line 27 (anti-slop differentiation table), 40 (substrate count), 77 (honest takeaway), 105 (Hooks section heading), 391 (project structure). New section preamble at line 107 explains the count breakdown by lifecycle event. (d) **README single-canonical Install**: second `## Install` heading renamed to `## Legacy installer reference` with explicit note that plugin route auto-loads `hooks/hooks.json` via `${CLAUDE_PLUGIN_ROOT}` substitution — no manual `settings.json` merge required. Closes the rafael CRIT-2 hidden-load-bearing-step framing. E2E validated 9 probes (4 secrets, 2 kb_scope provenance attack/legit, 3 Edit shape). contracts-validate: 0 violations.
- **H.5.1 (shipped — pattern status sync + KB exemption documentation)**: closes 2 of CS-3's findings via cheap drift-fix. (a) **Pattern status sync**: 9 patterns promoted from `status: implementing` → `status: active` across all 3 sources of truth (frontmatter, README index, SKILL.md table) — `asymmetric-challenger`, `trust-tiered-verification`, `agent-identity-reputation`, `shared-knowledge-base`, `content-addressed-refs`, `prompt-distillation`, `skill-bootstrapping`, `tech-stack-analyzer`, `structural-code-review`. Each was already shipped phases ago; status was just stale. SKILL.md table also added missing `kb-scope-enforcement` row (13th pattern, shipped H.4.0). Now: 12 active, 1 observed (`convergence-as-signal`), 0 implementing — accurate. (b) **Toolkit-meta KB exemption documented**: `kb-scope-enforcement.md` adds a new section explicitly noting that `kb:hets/{identity-roster, stack-skill-map, symmetric-pair-conventions}` are intentionally absent from any contract `kb_scope.default` because they describe shared orchestration vocabulary used by skills/commands at execution time, not per-persona domain knowledge. Closes the CS-3 "orphan KB" framing (which was wrong — they're not orphaned, just outside per-persona enforcement). The `kb_scope_consumed` check's principle is now explicit: "did the actor read its DOMAIN knowledge before producing findings?" — not "did every toolkit-shared doc get touched." Pattern table now in 3-way sync; contracts-validate reports 0 violations across all 7 validators.

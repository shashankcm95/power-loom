{
  "agentId": "actor-honesty-auditor",
  "persona": "05-honesty-auditor",
  "role": "actor",
  "skills": {
    "required": ["self-improve"],
    "recommended": ["prune", "agent-team"],
    "skill_status": {
      "self-improve": "available",
      "prune": "not-yet-authored",
      "agent-team": "available"
    }
  },
  "budget": {
    "tokens": 25000,
    "extensible": true,
    "maxExtensions": 1,
    "extensionAmount": 10000
  },
  "functional": [
    { "id": "F1", "check": "outputContainsFrontmatter", "required": true },
    { "id": "F2", "check": "frontmatterHasFields", "args": { "fields": ["id", "role", "depth", "parent", "persona"] }, "required": true },
    { "id": "F3", "check": "outputLengthMin", "args": { "min": 1500 }, "required": true },
    { "id": "F4", "check": "containsKeywords", "args": { "keywords": ["compliance", "transcript"] }, "required": true }
  ],
  "antiPattern": [
    { "id": "A1", "check": "noTextSimilarityToPriorRun", "args": { "threshold": 0.55 }, "severity": "warn" },
    { "id": "A2", "check": "noPaddingPhrases", "severity": "fail" },
    { "id": "A3", "check": "acknowledgesFallback", "severity": "warn" }
  ],
  "fallbackAcceptable": [
    "Transcript too long → strategic sampling with explicit grep/jq commands is acceptable"
  ]
}

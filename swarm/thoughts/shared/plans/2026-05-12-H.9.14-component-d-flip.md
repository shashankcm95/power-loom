---
phase: H.9.14
status: approved
gate_shape: mechanical-flip
triggers_present: 0
of_5_triggers: 0
manifest_bump: patch (1.15.0 → 1.15.1)
soak_counter_impact: unchanged
parent_plan: /Users/shashankchandrashekarmurigappa/.claude/plans/flickering-crafting-star.md
created: 2026-05-12
---

# Phase H.9.14 sub-plan — Component D flip: `kb-architecture-related-bidirectional` WARN-ONLY → HARD-violation

## Why this exists (parent-plan absorption)

Mirror of `/Users/shashankchandrashekarmurigappa/.claude/plans/flickering-crafting-star.md` (H.9.14 parent plan, approved 2026-05-12). Lives in `swarm/thoughts/shared/plans/` per H.9.10-onward precedent.

Mechanical-flip shape = NO architect / code-reviewer pre-approval. H.9.12-plan-time "INVOKED gate" labeling was speculative-conservative; re-evaluation post-H.9.13 yields 0-of-5 triggers in execution context (no fresh design surface; pre-decided at H.9.12; no schema change; no new option-axis; no institutional discipline encoding; LOW HIGH-class bug risk because baseline=0 + mechanical mirror of `pattern-related-bidirectional` L221-245).

## Empirical baseline (pre-implementation; recorded 2026-05-12 post-H.9.13)

- origin/main at `885e9ac` (H.9.13 ship hash record); H.9.13 substantive at `6d71fb8`
- Smoke: 85/85 (post-H.9.13 unchanged)
- _h70-test: 67/67
- contracts-validate: 17 baseline total violations (all from `contract-plugin-hook-deployment`; installed plugin lag at 1.1.3 vs substrate 1.15.0)
- `kb-architecture-related-bidirectional`: 0 violations (asymmetric=0 post-H.9.13 cohort closure)
- WARN-ONLY stderr: absent (asymmetric=0 → no warning emitted)
- ESLint: 0 errors
- Markdownlint: 0 errors
- Plugin manifest: 1.15.0

## Component A — Flip implementation in contracts-validate.js

### Current state (L287-322)

```javascript
validators['kb-architecture-related-bidirectional'] = function () {
  // WARN-ONLY: violations array intentionally always empty for H.9.12.
  // H.9.13 closure flips this to hard-violation mode.
  const violations = [];
  const relatedMap = new Map();
  for (const { kbId, path: fp } of listKbArchitectureFiles()) {
    try {
      const { frontmatter: fm } = parseFrontmatter(fs.readFileSync(fp, 'utf8'));
      const related = Array.isArray(fm.related) ? fm.related : (fm.related ? [fm.related] : []);
      relatedMap.set(kbId, new Set(related));
    } catch { /* corrupt frontmatter or read error; skip — fail-soft */ }
  }
  const asymmetricLinks = [];
  for (const [kbId, related] of relatedMap.entries()) {
    for (const target of related) {
      if (!relatedMap.has(target)) continue;
      const reverse = relatedMap.get(target);
      if (!reverse.has(kbId)) {
        asymmetricLinks.push({ from: kbId, to: target });
      }
    }
  }
  // Emit stderr warnings for visibility (institutional discipline encoding);
  // drift-note 82 cohort captured for H.9.13 mass-fix.
  if (asymmetricLinks.length > 0) {
    process.stderr.write(`  ⚠ kb-architecture-related-bidirectional (WARN-ONLY MODE per H.9.12; drift-note 82 cohort): ${asymmetricLinks.length} asymmetric link(s) — cohort for H.9.13 mass-fix\n`);
    for (const link of asymmetricLinks.slice(0, 10)) {
      process.stderr.write(`      ${link.from} → ${link.to} (no back-link)\n`);
    }
    if (asymmetricLinks.length > 10) {
      process.stderr.write(`      ... and ${asymmetricLinks.length - 10} more (see drift-note 82 for full cohort)\n`);
    }
  }
  return violations;
};
```

### Post-H.9.14 state

```javascript
validators['kb-architecture-related-bidirectional'] = function () {
  // H.9.14 FLIP: WARN-ONLY → HARD-violation mode following warn-only-then-
  // fix-then-flip pattern (H.9.12 introduced WARN-ONLY surfacing 23 asymmetric;
  // H.9.13 closed cohort to asymmetric=0; H.9.14 flips to HARD with baseline=0
  // for zero-regression flip + regression protection against re-introduction).
  // Mirrors pattern-related-bidirectional (L221-245); same shape; same severity.
  const violations = [];
  const relatedMap = new Map();
  for (const { kbId, path: fp } of listKbArchitectureFiles()) {
    try {
      const { frontmatter: fm } = parseFrontmatter(fs.readFileSync(fp, 'utf8'));
      const related = Array.isArray(fm.related) ? fm.related : (fm.related ? [fm.related] : []);
      relatedMap.set(kbId, new Set(related));
    } catch { /* corrupt frontmatter or read error; skip — fail-soft */ }
  }
  for (const [kbId, related] of relatedMap.entries()) {
    for (const target of related) {
      // Skip references outside kb/architecture tree (cross-tree refs allowed)
      if (!relatedMap.has(target)) continue;
      const reverse = relatedMap.get(target);
      if (!reverse.has(kbId)) {
        violations.push({
          kind: 'asymmetric-related-link',
          from: kbId,
          to: target,
          fix: `Add "${kbId}" to ${target}.md frontmatter "related" array`,
        });
      }
    }
  }
  return violations;
};
```

**Net change**:
- Removed: WARN-ONLY comment (2 lines) + asymmetricLinks intermediate array (5 lines reused as violations push)
- Removed: stderr emission block (10 lines)
- Added: violations.push with kind/from/to/fix (5 lines)
- Updated: top-of-function comment block (4 lines: H.9.14 flip history)

**Net LoC**: ~-12 / +9 = -3 net.

### Comment block update (top-of-function area L270-286)

Replace H.9.12 WARN-ONLY exposition with H.9.14 FLIP-complete exposition (still mentions H.9.13 cohort closure for historical context).

## Component B — Test 89 implementation in smoke-ht.sh

### Test structure (after Test 88 in smoke-ht.sh)

```bash
# Test 89: H.9.14 kb-architecture-related-bidirectional HARD-violation regression
# Verifies that the validator now HARD-fires (was WARN-ONLY at H.9.12) on
# asymmetric link injection. Synthetic fault: remove one back-link entry
# from single-responsibility.md, expect contracts-validate to report
# asymmetric-related-link violations + Total >= 18. Restore + verify baseline.
test_h914_component_d_flip_regression() {
  local target_doc="${TOOLKIT_ROOT}/skills/agent-team/kb/architecture/crosscut/single-responsibility.md"
  
  # Capture + mutate + run + restore + re-verify via Node (portable across BSD/Linux per H.9.12.1 lesson)
  local probe_result
  probe_result=$(node -e "
    const fs = require('fs');
    const { execSync } = require('child_process');
    const path = '$target_doc';
    const orig = fs.readFileSync(path, 'utf8');
    // Remove one back-link entry: architecture/ai-systems/agent-design (added at H.9.13 B1)
    const mutated = orig.replace(/^  - architecture\\/ai-systems\\/agent-design\\n/m, '');
    if (mutated === orig) { console.log('FAIL: mutation no-op'); process.exit(1); }
    fs.writeFileSync(path, mutated);
    // Run contracts-validate
    let output = '';
    let exitCode = 0;
    try {
      output = execSync('node ${TOOLKIT_ROOT}/scripts/agent-team/contracts-validate.js 2>&1', { encoding: 'utf8' });
    } catch (e) {
      output = e.stdout?.toString() || e.message;
      exitCode = e.status || 1;
    }
    // Restore BEFORE asserting (defensive: ensure cleanup even if assert fails)
    fs.writeFileSync(path, orig);
    // Assertions: exit 1 + asymmetric count >=1 + Total >=18
    const asymCount = (output.match(/asymmetric-related-link/g) || []).length;
    const totalMatch = output.match(/Total violations: (\\d+)/);
    const total = totalMatch ? parseInt(totalMatch[1]) : 0;
    if (exitCode !== 1) { console.log('FAIL: expected exit 1 got ' + exitCode); process.exit(1); }
    if (asymCount < 1) { console.log('FAIL: expected >=1 asymmetric violation got ' + asymCount); process.exit(1); }
    if (total < 18) { console.log('FAIL: expected Total >=18 got ' + total); process.exit(1); }
    // Re-run after restore to verify baseline
    let baselineOutput = '';
    let baselineExit = 0;
    try {
      baselineOutput = execSync('node ${TOOLKIT_ROOT}/scripts/agent-team/contracts-validate.js 2>&1', { encoding: 'utf8' });
    } catch (e) {
      baselineOutput = e.stdout?.toString() || e.message;
      baselineExit = e.status || 1;
    }
    const baselineAsym = (baselineOutput.match(/asymmetric-related-link/g) || []).length;
    const baselineTotalMatch = baselineOutput.match(/Total violations: (\\d+)/);
    const baselineTotal = baselineTotalMatch ? parseInt(baselineTotalMatch[1]) : 0;
    if (baselineAsym !== 0) { console.log('FAIL: baseline asymmetric expected 0 got ' + baselineAsym); process.exit(1); }
    if (baselineTotal !== 17) { console.log('FAIL: baseline Total expected 17 got ' + baselineTotal); process.exit(1); }
    console.log('OK (fault: exit=' + exitCode + ' asym=' + asymCount + ' total=' + total + '; baseline: asym=' + baselineAsym + ' total=' + baselineTotal + ')');
  " 2>&1)
  
  if echo "$probe_result" | grep -q "^OK"; then
    echo "  Test 89 (H.9.14 kb-architecture-related-bidirectional HARD-violation regression; fault injection + restore): ${probe_result}"
    passed=$((passed + 1))
  else
    echo "  Test 89 (H.9.14 kb-architecture-related-bidirectional HARD-violation regression): FAIL: ${probe_result}"
    failed=$((failed + 1))
  fi
}
test_h914_component_d_flip_regression
```

**Critical defenses**:
- Node-based mutation (portable across BSD/Linux per H.9.12.1 stat-cascade lesson)
- Restore BEFORE asserting (defensive: cleanup even if assertion fails — ensures no permanent doc state change on test failure)
- Two-stage verification: fault → baseline (catches insufficient cleanup OR validator non-determinism)
- Lowercase `passed`/`failed` counters matching Test 85 + Test 86 + Test 88 precedent

## Component C — Plugin manifest patch bump

`.claude-plugin/plugin.json`: `"version": "1.15.0"` → `"version": "1.15.1"`.

Rationale documented in parent plan.

## Component D — CHANGELOG + SKILL.md + HT-state.md cutover

Standard ledger format per H.9.13 precedent. Mechanical mirror.

## Verification cadence

After each substantive edit:
1. Component A flip → `node scripts/agent-team/contracts-validate.js` → expect 17-baseline + asymmetric=0 + NO WARN-ONLY stderr
2. Component A flip → `npx eslint scripts/agent-team/contracts-validate.js` → 0 errors
3. Component B Test 89 → `bash install.sh --hooks --test` → 86/86 (was 85/85)
4. Final pre-commit: smoke 86/86 + _h70-test 67/67 + 17-baseline contracts + markdownlint 0 errors

## Forbidden-phrase audit

Sub-plan research-artifact-scoped (planning prose). No "robust" / "comprehensive" / "production-grade" / "just X" / "best practice" / speculative-outcomes-as-fact.

## Flag absorption record

No pre-approval gate fired per 0-of-5 trigger analysis. Empirical safety net replaces formal gate.

## Estimated completion

~1.5-2h wallclock. Risk: LOW.

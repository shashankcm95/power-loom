---
date: 2026-05-09T23:30:00-07:00
researcher: root
git_commit: b513bd1
branch: main
repository: power-loom
topic: "HT.0.3 — Slash commands audit"
tags: [research, hardening-track, slash-commands, build-team, build-plan, verify-plan, research, implement]
status: complete
last_updated: 2026-05-09
last_updated_by: root
---

# HT.0.3 — Slash commands audit

Documentary audit of the 13 slash command files at `commands/*.md` per master plan v3.1 methodology. 5 quality bars applied (A. dead code, B. architectural compliance, C. bloat, D. optimization, E. gap analysis). Special focus per master plan: (a) each command's documented flow references actual scripts/files (all refs resolve); (b) command-to-callsite mapping with documented when-to-use/when-not-to-use; (c) `commands/build-team.md` Step 1.5 (H.8.5) wiring intact; (d) `commands/research.md` + `commands/implement.md` (H.8.6) workflow consistency; (e) deprecated patterns flagged; (f) token budget per command.

## Progress marker

13 of 13 command files inventoried. Forbidden-phrase grep gate run pre-commit (results captured below).

## Inventory

| File | LoC | Phase tags carried | Cross-references |
|------|-----|--------------------|--------------------|
| `commands/security-audit.md` | 11 | none | `security-auditor` agent; `npm audit` |
| `commands/review.md` | 12 | none | `code-reviewer` agent; `git diff` |
| `commands/plan.md` | 20 | H.7.9 (relationship to /build-plan) | `planner` agent; `/build-plan` cross-link; `.claude/plans/` |
| `commands/prune.md` | 38 | none | `MEMORY.md`; `~/.claude/rules/toolkit/`; `~/.claude/agents/`; `~/.claude/skills/`; MemPalace |
| `commands/evolve.md` | 41 | none | `~/.claude/agents/`; `~/.claude/skills/`; `~/Documents/claude-toolkit/`; `/forge`; MemPalace |
| `commands/forge.md` | 42 | none | `~/.claude/agents/`; `~/.claude/skills/`; both-locations write pattern; MemPalace |
| `commands/self-improve.md` | 45 | none | `MEMORY.md`; `~/.claude/rules/toolkit/`; `~/.claude/skills/`; `~/.claude/agents/`; MemPalace |
| `commands/verify-plan.md` | 54 | H.7.23 (entry point); H.7.22 (drift-note 40 codification) | `skills/verify-plan/SKILL.md`; `scripts/agent-team/verify-plan-spawn.js`; `validate-plan-schema.js`; `patterns/plan-mode-hets-injection.md` |
| `commands/chaos-test.md` | 99 | H.2-bridge (identity assignment); H.2.4 (trust-tier); H.2.9 (--pattern); H.7.x (paths) | `super-agent.md`; `agent-identity.js`; `tree-tracker.js`; `contract-verifier.js`; `hierarchical-aggregate.js`; `compliance-probe.sh`; `pattern-runner.js` |
| `commands/research.md` | 116 | H.8.6 (RPI workflow); humanlayer canonical | `agent-identity.js`; `swarm/thoughts/shared/research/`; personas 14/15/16 |
| `commands/implement.md` | 132 | H.8.6 (RPI workflow); humanlayer canonical; ace-fca.md | `swarm/thoughts/shared/plans/`; `install.sh`; `contracts-validate.js`; `_h70-test.js` |
| `commands/build-plan.md` | 157 | H.7.9 (entry); H.7.3 (route-decide); H.7.5 (context-aware) | `route-decide.js`; `agents/planner.md`; `~/.claude/plans/`; `swarm/plan-template.md`; `skills/build-plan/SKILL.md` |
| `commands/build-team.md` | 322 | H.2.5 (entry); H.7.3+H.7.5 (Step 0); H.5.7 (task-type heuristic); H.6.5 (capability requests); H.8.5 (Step 1.5 wiring) | `route-decide.js`; `kb-resolver.js`; `agent-identity.js`; `pattern-recorder.js`; `contract-verifier.js`; `build-spawn-context.js`; `tech-stack-analyzer/SKILL.md` |
| **Total** | **1089** | | **13 files** |

Master plan estimate was "~12 files"; actual count is 13 (the master plan listed 13 explicit names: build-plan, build-team, chaos-test, evolve, forge, implement, plan, prune, research, review, security-audit, self-improve, verify-plan; the "~12" was a typo — all 13 listed names are present).

Total command LoC is 1089 — 11.07% of HT.0.2's 9839 LoC surface and 31.1% of HT.0.1's 3500 LoC. Smallest audit phase by LoC to date.

## Findings — Bar A (dead code)

Bar A applies the mechanical 3-step: (1) every script/agent/skill referenced exists; (2) every cross-reference resolves; (3) no orphan link targets.

### Script existence verification

All scripts referenced across the 13 commands resolve to existing files:

| Reference | Path | Exists |
|-----------|------|--------|
| `npm audit` | external tool | n/a |
| `git diff` / `git log` | external tool | n/a |
| `scripts/agent-team/route-decide.js` | `scripts/agent-team/route-decide.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/agent-identity.js` | `scripts/agent-team/agent-identity.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/tree-tracker.js` | `scripts/agent-team/tree-tracker.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/contract-verifier.js` | `scripts/agent-team/contract-verifier.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/pattern-recorder.js` | `scripts/agent-team/pattern-recorder.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/pattern-runner.js` | `scripts/agent-team/pattern-runner.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/kb-resolver.js` | `scripts/agent-team/kb-resolver.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/build-spawn-context.js` | `scripts/agent-team/build-spawn-context.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/contracts-validate.js` | `scripts/agent-team/contracts-validate.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/_h70-test.js` | `scripts/agent-team/_h70-test.js` | yes (HT.0.2 inventory) |
| `scripts/agent-team/verify-plan-spawn.js` | `scripts/agent-team/verify-plan-spawn.js` | yes (HT.0.2 inventory) |
| `swarm/hierarchical-aggregate.js` | `swarm/hierarchical-aggregate.js` | yes (HT.0.2 inventory) |
| `compliance-probe.sh` | `scripts/compliance-probe.sh` AND `~/.claude/scripts/compliance-probe.sh` | yes (both locations) |
| `validate-plan-schema.js` | `hooks/scripts/validators/validate-plan-schema.js` | yes (HT.0.1 inventory) |
| `install.sh` | `install.sh` (repo root) | yes |
| `swarm/plan-template.md` | `swarm/plan-template.md` | yes |

### Agent existence verification

All agents referenced across commands resolve to existing files at `agents/*.md`:

| Reference | Path | Referenced from |
|-----------|------|------------------|
| `security-auditor` | `agents/security-auditor.md` | `security-audit.md:9` |
| `code-reviewer` | `agents/code-reviewer.md` | `review.md:10` |
| `planner` | `agents/planner.md` | `plan.md:7,16`; `build-plan.md:3,98` |
| `architect` | `agents/architect.md` | (referenced indirectly via "architect persona" in build-plan.md, build-team.md) |
| `optimizer` | `agents/optimizer.md` | (no command references; standalone agent) |

5 agents exist at `agents/`. The `optimizer` agent has no command references in the audit scope.

### Skill existence verification

All skills referenced via `../skills/<name>/SKILL.md` resolve:

| Skill | Path | Referenced from |
|-------|------|------------------|
| `verify-plan` | `skills/verify-plan/SKILL.md` | `verify-plan.md:3,27,54` |
| `build-plan` | `skills/build-plan/SKILL.md` | `build-plan.md:1,156` |
| `tech-stack-analyzer` | `skills/tech-stack-analyzer/SKILL.md` | `build-team.md:3,151` |
| `agent-team` (patterns) | `skills/agent-team/patterns/*.md` | `verify-plan.md:54`; `chaos-test.md:56,58`; `research.md:3` |

The `skills/` directory contains 16 skill packages (per filesystem listing); 4 are referenced from commands. The remaining 12 skill packages (agent-swarm, airflow, deploy-checklist, fullstack-dev, kubernetes, node-backend-development, penetration-testing, prompt-enrichment, react, research-mode, self-improve, skill-forge, swift-development) are not directly referenced from `commands/*.md` but may be referenced from agents, hooks, or other infrastructure.

### Persona reference verification

`commands/research.md:54-56` references documentary personas `14-codebase-locator`, `15-codebase-analyzer`, `16-codebase-pattern-finder`. Per HT-state.md, these are documentary contracts shipped at H.8.6. Per master plan HT.0.4 scope (line 270-294), the persona MD asymmetry (14/15/16 contracts exist; persona MD docs do not) is an HT.0.4 audit item.

### Cross-command links

Inter-command cross-references found:

- `plan.md` → `/build-plan` (line 7-9, with explicit cross-link `[/build-plan](build-plan.md)`)
- `build-plan.md` → `/plan` (line 5: "Step 0's `root` recommendation redirects cleanly to `/plan`")
- `verify-plan.md` → `/plan` and `/build-plan` (line 38-42: trust-model relationship)
- `research.md` → `/plan` (line 99-103: "Next step: `/plan` consuming this artifact")
- `implement.md` → `/plan` and `/research` (line 113-115: "What this command is NOT")
- `evolve.md` → `/forge` (line 14: "If not found, suggest using `/forge` instead")
- `build-team.md` → `/chaos-test`, `/plan`, `/forge` (line 313-318: "What this command is NOT" + line 295: forge-skill request)
- `chaos-test.md` → `/forge` (line 90: "A separate `/forge` or manual approval kicks off the fix phase")

All cross-command links resolve to existing command files.

## Findings — Bar B (architectural compliance)

Bar B verifies cross-reference patterns, when-to-use discipline, both-locations-write pattern, path-resolution conventions.

### When-to-use / When-not-to-use sections

| Command | Has When-to-use | Has What-this-is-NOT / When-NOT |
|---------|-----------------|-----------------------------------|
| `security-audit.md` | no | no |
| `review.md` | no | no |
| `plan.md` | yes (line 5-9) | no |
| `prune.md` | no | no |
| `evolve.md` | no | no |
| `forge.md` | no | no |
| `self-improve.md` | no | no |
| `verify-plan.md` | yes (line 19-23, "When to invoke") | yes (line 36-42, "Why this is additive") |
| `chaos-test.md` | no (only "Why a hierarchical chaos test?" rationale) | no |
| `research.md` | yes (line 16-27, "CRITICAL — discipline of this command") | yes (line 105-110, "What this command is NOT") |
| `implement.md` | yes (Steps 1-6 carry per-step discipline) | yes (line 111-116, "What this command is NOT") |
| `build-plan.md` | yes (line 5: "Relationship to `/plan`") | implicit (Step 0 redirects to `/plan`) |
| `build-team.md` | yes (line 23: `--force-route` flag; line 70-106: Step 0 routing) | yes (line 309-314, "What this command is NOT") |

6 of 13 commands carry explicit When-to-use sections; 4 of 13 carry explicit What-this-is-NOT sections. The 7 commands lacking When-to-use are the older / smaller ones (security-audit, review, prune, evolve, forge, self-improve, chaos-test). The 4 commands carrying What-this-is-NOT are the H.7.x and H.8.x era commands (verify-plan, research, implement, build-team).

### Cross-reference path conventions

The 13 commands use four distinct path-reference conventions:

| Convention | Example | Used by |
|-----------|---------|---------|
| Repo-relative | `scripts/agent-team/route-decide.js` | `verify-plan.md`; `build-plan.md`; `implement.md`; `research.md` |
| Hardcoded author-machine | `~/Documents/claude-toolkit/scripts/agent-team/X.js` | `chaos-test.md` (lines 18, 21, 24, 43, 56, 57, 65, 79); `research.md` (lines 62-67) |
| `$HOME`-aware | `$HOME/Documents/claude-toolkit/scripts/agent-team/route-decide.js` | `build-plan.md:31`; `build-team.md:32` |
| Relative path (`../`) | `../skills/verify-plan/SKILL.md` | `verify-plan.md:3,54`; `build-plan.md:156`; `build-team.md:3,151`; `research.md:3` |
| Deployed-marketplace | `~/.claude/scripts/compliance-probe.sh` | `chaos-test.md:80` |

Inconsistency observed:

- `chaos-test.md` mixes hardcoded author-machine paths (`~/Documents/claude-toolkit/...`) with deployed-marketplace paths (`~/.claude/scripts/compliance-probe.sh`) in the same file (lines 18 vs 80).
- `build-team.md` uses `$HOME`-aware paths (line 32), while `chaos-test.md` uses literal `~/`.
- `forge.md` and `evolve.md` documentation explicitly references both-locations-write pattern: write to `~/Documents/claude-toolkit/{type}/{name}.md` AND `~/.claude/{type}/{name}.md` (lines 28-33 in forge.md; lines 31-33 in evolve.md). These are documentation patterns for the human reader.

### Both-locations-write pattern

`forge.md` and `evolve.md` document the both-locations write pattern:
- `forge.md:27-33` — for new agents/skills, write both to `~/Documents/claude-toolkit/{agents|skills}/...` (repo source) and `~/.claude/{agents|skills}/...` (active install)
- `evolve.md:31-33` — for updates, sync both locations

The pattern predates the H.7.14 `findToolkitRoot()` extraction (which substrate scripts use). Command docs continue to use hardcoded author-machine paths as the documentation convention; substrate scripts use `findToolkitRoot()` for runtime resolution.

### `/build-plan` Step 0 redirect to `/plan`

`build-plan.md:60-87` Step 0 emits "Re-invoke as /plan, or use /build-plan --force-plan if you disagree" when `route-decide.js` returns `root`. Cross-reference is intact: `/plan` exists and accepts the redirect cleanly.

### Forcing-instruction handling in commands

`build-team.md:54-68` and `build-plan.md:50-58` both handle the `[ROUTE-DECISION-UNCERTAIN]` forcing instruction (per H.7.5). When `route-decide.js` emits `uncertain: true`, both commands surface the instruction to the user and refuse to silently default. Pattern matches the forcing-instruction-family Convention G class 1 advisory shape (per HT.0.1 forcing-instruction inventory).

## Findings — Bar C (bloat)

Bar C tests purpose-fit, file-size envelope, comment density.

### Command-size distribution

| LoC range | Commands | Pattern |
|-----------|----------|---------|
| 11-20 | security-audit, review, plan | Thin entry points (≤20 LoC); rely on referenced agents/commands for behavior |
| 38-54 | prune, evolve, forge, self-improve, verify-plan | Mid-size workflow commands (38-54 LoC); 4-6 numbered steps each |
| 99-157 | chaos-test, research, implement, build-plan | Substantial commands (99-157 LoC); multiple subsections with embedded bash blocks |
| 322 | build-team | Largest command; bundles route-decide gate (Step 0) + pre-flight (Step 1) + spawn-context auto-extension (Step 1.5) + tech-stack-analyzer (Step 2) + per-identity verification with three branches (Step 7) + capability-request handling (Step 4) |

`build-team.md` at 322 LoC is 6.45× the median command size (47 LoC) and 29.3× the smallest. The file bundles 6 phase-tagged sections (H.2.5, H.7.3, H.7.5, H.5.7, H.6.5, H.8.5).

### Embedded bash code-block density

| Command | Embedded bash LoC | % of file |
|---------|--------------------|-----------|
| `build-team.md` | ~145 LoC of bash | ~45% |
| `build-plan.md` | ~60 LoC of bash | ~38% |
| `chaos-test.md` | ~25 LoC of bash | ~25% |
| `research.md` | ~10 LoC of bash | ~9% |
| `implement.md` | ~10 LoC of bash | ~8% |

The bash blocks in `build-team.md` and `build-plan.md` are documentary (per build-team.md line 89-92: "shell-driven prompts in /build-team are model-followed, not literally interactive"). Each command is loaded into LLM context when invoked; the bash blocks contribute substantial token cost.

### Multi-responsibility bundling at one command

`build-team.md` bundles 6 distinct responsibilities (parallel to HT.0.1's hooks finding and HT.0.2's substrate-scripts finding):

- (a) Route-decide gate (Step 0; H.7.3 + H.7.5)
- (b) Pre-flight check (Step 1)
- (c) Spawn-context auto-extension (Step 1.5; H.8.5)
- (d) Tech-stack-analyzer invocation (Step 2)
- (e) Per-identity verification with three trust-tier branches (Step 7; H.2.4 + H.7.1)
- (f) Capability-request handling for sub-agent forge requests (Step 4; H.6.5)

Articulate-or-split decision for HT.0.9 (parallel to HT.0.1's `auto-store-enrichment.js` / HT.0.2's `agent-identity.js` findings).

## Findings — Bar D (optimization)

Bar D is static-only per master plan: token cost per command (markdown context tax).

### Token-cost-per-invocation observations

Each command's full markdown content is loaded into LLM context when the slash command fires. Token cost scales with command LoC (rough approximation: 1 LoC ≈ 8-15 tokens depending on bash-block density).

| Command | LoC | Estimated token cost | Multiplier vs smallest |
|---------|-----|----------------------|-------------------------|
| `security-audit.md` | 11 | ~150 tokens | 1× |
| `review.md` | 12 | ~160 tokens | 1.07× |
| `plan.md` | 20 | ~270 tokens | 1.8× |
| `prune.md` | 38 | ~520 tokens | 3.5× |
| `evolve.md` | 41 | ~560 tokens | 3.7× |
| `forge.md` | 42 | ~580 tokens | 3.9× |
| `self-improve.md` | 45 | ~620 tokens | 4.1× |
| `verify-plan.md` | 54 | ~750 tokens | 5× |
| `chaos-test.md` | 99 | ~1400 tokens | 9.3× |
| `research.md` | 116 | ~1600 tokens | 10.7× |
| `implement.md` | 132 | ~1800 tokens | 12× |
| `build-plan.md` | 157 | ~2200 tokens | 14.7× |
| `build-team.md` | 322 | ~5000 tokens | 33.3× |

Total command-doc token surface across the 13 commands: ~15,000 tokens. Each command is loaded ON-INVOKE-ONLY (not all-at-once); the per-invocation cost is the LoC of the single command being invoked.

### Bash-block contribution to per-command token cost

Per Bar C's bash-density table, `build-team.md` (~45% bash) and `build-plan.md` (~38% bash) carry the densest embedded bash blocks. Each bash block is contextual documentation for the model reader; collapsing or externalizing them is an HT.0.9 sizing decision parallel to HT.0.2's `architecture-relevance-detector.js` ROUTING_RULES inline-data observation.

## Findings — Bar E (gap analysis)

Bar E checks stated source vs current actual. Source-priority order: `swarm/adrs/*` > `kb/architecture/**.md` > pattern docs > `SKILL.md` > inline command comments.

### Phase-tag coverage vs current substrate

`build-team.md` phase tags: H.2.5 (entry), H.7.3+H.7.5 (Step 0), H.5.7 (task-type heuristic), H.6.5 (capability requests), H.8.5 (Step 1.5 wiring). All five phase tags are covered in current substrate per HT.0.1 + HT.0.2 inventories.

`verify-plan.md` phase tags: H.7.23 (entry; drift-note 40 codification), H.7.22 (referenced in Trust model section). Both covered.

`research.md` + `implement.md` phase tags: H.8.6 (RPI workflow). Covered. Both reference humanlayer canonical (`humanlayer/advanced-context-engineering-for-coding-agents`); the citation is to an external repository, not internal substrate.

`build-plan.md` phase tags: H.7.9 (entry), H.7.3 (route-decide), H.7.5 (context-aware). Covered.

`plan.md` phase tag: H.7.9 (relationship-to-build-plan section). Covered.

`chaos-test.md` phase tags: H.2-bridge (identity assignment), H.2.4 (trust-tier), H.2.9 (`--pattern`). Covered.

Seven commands carry no phase tags: `security-audit.md`, `review.md`, `prune.md`, `evolve.md`, `forge.md`, `self-improve.md`. These predate the H.x convention (reference HT.0.2's parallel observation for `prompt-pattern-store.js` Phase-X tag era).

### `commands/build-team.md` Step 1.5 (H.8.5) wiring intact (master plan special focus)

Verified at `commands/build-team.md:122-148`. Step 1.5 invokes `node ~/Documents/claude-toolkit/scripts/agent-team/build-spawn-context.js` (line 138) with `--task` and `--format text` arguments. The `build-spawn-context.js` script exists and is callable per HT.0.2 audit (lines 60-148 of that file compose detector + adr + kb-resolver). Wiring is intact.

The fail-open path (line 134-141: "Per ADR-0001, this is fail-open: if the helper errors, SPAWN_CONTEXT becomes empty string and Step 2 proceeds without substrate-curated anchoring") matches `build-spawn-context.js`'s fail-open discipline (per HT.0.2 audit line 282-285 of that file).

### `commands/research.md` + `commands/implement.md` (H.8.6) workflow consistency (master plan special focus)

Verified. The RPI handoff is consistent across both commands:

- `research.md:76-95` writes research artifact to `swarm/thoughts/shared/research/<DATE>-<DESC>.md` with required frontmatter
- `implement.md:27-30` reads plan files from `swarm/thoughts/shared/plans/`, then reads the plan's `research_artifact:` frontmatter field for ground-truth context
- `swarm/plan-template.md` exists and defines the plan schema (per file existence check)

The handoff path is: `/research` → research artifact at `swarm/thoughts/shared/research/` → `/plan` (or `/build-plan`) writes plan with `research_artifact:` field → `/implement` reads plan + linked research artifact.

The discipline matches: `research.md:18-25` enforces "DO NOT critique" (documentary discipline); `implement.md:51-62` enforces "If you encounter a mismatch — STOP. Report clearly" (mismatch escalation rather than silent drift).

### Deprecated patterns (master plan special focus)

No commands reference superseded helpers. Verified by cross-referencing the command-doc references against HT.0.1 (hooks layer) + HT.0.2 (substrate scripts) inventories. All referenced scripts/agents/skills are current.

`commands/plan.md:7-9` notes the relationship to `/build-plan` (H.7.9 introduced both; `/plan` remains the thin delegate). This is documentary explanation, not deprecation — the two coexist.

The auto-loop infrastructure for self-improvement (per `~/.claude/rules/toolkit/core/self-improvement.md` H.4.1) is hook-based (3 hook integrations: `auto-store-enrichment.js`, `pre-compact-save.js`, `session-self-improve-prompt.js` per HT.0.1 inventory). `commands/self-improve.md` does not reference this auto-loop infrastructure or H.4.1; the command doc describes the explicit triage workflow (Memory→Rule promotion). The auto-loop and the explicit /self-improve command coexist (per the rule file's framing: auto-loop runs continuously via the 3 hook integrations; explicit `/self-improve` is reserved for the load-bearing Memory→Rule promotion). Documentary observation: `self-improve.md` predates H.4.1 and carries no forward-link to it.

### Token-budget-per-command (master plan special focus)

Per Bar D: token cost ranges from ~150 tokens (`security-audit.md`) to ~5000 tokens (`build-team.md`), with median at ~620 tokens (`self-improve.md`). The 33.3× spread between smallest and largest is driven by `build-team.md`'s embedded bash blocks (~145 LoC of bash; ~45% of the file).

### Explicit `--option` flag mapping vs current behavior

| Flag | Command | Behavior in command doc |
|------|---------|---------------------------|
| `--force-route` | `build-team.md:23` | "skips Step 0 entirely and proceeds to Step 1" |
| `--force-root` | `build-team.md:66` | route-decide-uncertain handling |
| `--force-plan` | `build-plan.md:14,26` | "skips Step 0 gate" |
| `--skip-hets` | `build-plan.md:16,121` | "skips Step 3 architect recommendation" |
| `--skip-fixes` | `verify-plan.md:15` | "surface findings only, don't apply fixes inline" |
| `--max-depth N` | `chaos-test.md:8` | "limit recursion (default 3; 2 means flat swarm)" |
| `--no-baseline` | `chaos-test.md:9` | "skip cross-run delta even if prior runs exist" |
| `--pattern <name>` | `chaos-test.md:10` (H.2.9) | pattern-targeted run |

The flags listed in command docs match the substrate-script CLI conventions (`route-decide.js` accepts `--force-route` / `--force-root` / `--context`; `pattern-runner.js` accepts `--pattern <name>`). Flag-to-callsite alignment is consistent.

## Findings — special focus areas

### Each command's documented flow references actual scripts/files (all refs resolve)

Verified in Bar A. All 17+ unique script paths, all 5 agent paths, all 4 skill paths, plus `compliance-probe.sh` (both deployed and repo-internal copies), `install.sh`, `swarm/plan-template.md`, and the personas-contracts directory all exist. All references resolve to existing targets.

### Command-to-callsite mapping with documented when-to-use/when-not-to-use

Per Bar B's table: 6 of 13 commands carry explicit When-to-use; 4 carry What-this-is-NOT. The 7 commands lacking When-to-use are pre-H.7.x era commands (security-audit, review, prune, evolve, forge, self-improve, chaos-test). The H.7.x and H.8.x era commands carry the modern discipline.

### `commands/build-team.md` Step 1.5 (H.8.5) wiring intact

Verified above. The wiring at `build-team.md:122-148` invokes `build-spawn-context.js`, which exists and follows ADR-0001 fail-open discipline.

### `commands/research.md` + `commands/implement.md` (H.8.6) workflow consistency

Verified above. The RPI handoff path is consistent end-to-end.

### Deprecated patterns flagged

None found. All referenced substrate is current.

### Token budget per command

Documented in Bar D. `build-team.md` is the densest at ~5000 tokens; total command-doc surface is ~15,000 tokens (per-invocation load is the single command's LoC, not all-at-once).

## Follow-up questions for plan phase

13 items surfaced during this audit; documented for HT.0.9 synthesis categorization (ADR-update / mechanical-fix / refactor-candidate / accept-and-document):

1. **`commands/build-team.md` 322 LoC bundles 6 responsibilities at one file** — route-decide gate + pre-flight + spawn-context auto-extension + tech-stack-analyzer + per-identity verification (3 branches) + capability-request handling. Articulate-or-split decision for HT.0.9 (parallel to HT.0.1's hooks finding + HT.0.2's `agent-identity.js` finding).

2. **Path-reference convention inconsistency across commands** — 5 distinct conventions in use: repo-relative, hardcoded author-machine (`~/Documents/claude-toolkit/...`), `$HOME`-aware, relative-path (`../`), deployed-marketplace (`~/.claude/...`). `chaos-test.md` mixes two of them in the same file (lines 18 vs 80). Consolidation candidate.

3. **7 commands lack explicit When-to-use sections** — `security-audit.md`, `review.md`, `prune.md`, `evolve.md`, `forge.md`, `self-improve.md`, `chaos-test.md`. These predate the H.7.x convention. Mechanical-fix or accept-and-document.

4. **4 commands lack What-this-is-NOT sections** — same predates-H.7.x cohort minus `verify-plan.md` and `build-plan.md` (which have implicit redirects). Mechanical-fix.

5. **7 commands carry no phase tags** — same cohort. Documentary observation; matches HT.0.2's `prompt-pattern-store.js` Phase-X-tag era pattern.

6. **`commands/self-improve.md` predates H.4.1 auto-loop infrastructure** — the command doc describes only the explicit triage workflow; does not link forward to the H.4.1 hook-based auto-loop (per `rules/toolkit/core/self-improvement.md`). Documentary observation; mechanical-fix candidate (add forward-link).

7. **`commands/forge.md` + `commands/evolve.md` document hardcoded author-machine paths** for both-locations-write pattern — `~/Documents/claude-toolkit/...`. The substrate scripts use `findToolkitRoot()` for runtime resolution; the docs use the author-path as the readable convention. Documentary observation.

8. **Command token cost spread** — 33.3× between smallest (security-audit ~150 tokens) and largest (build-team ~5000 tokens). `build-team.md` is ~45% embedded bash. Externalize-or-collapse decision for HT.0.9 (parallel to HT.0.2's inline-data observations on `architecture-relevance-detector.js` and `route-decide.js`).

9. **`commands/research.md:54-56` references documentary personas 14/15/16** — the persona MD asymmetry (contracts exist, persona MD docs do not) is an HT.0.4 audit item per master plan line 270-294. Cross-reference for HT.0.4 inventory.

10. **`commands/chaos-test.md:80` uses `~/.claude/scripts/compliance-probe.sh` (deployed-marketplace path)** while the same file uses `~/Documents/claude-toolkit/scripts/agent-team/...` (author-machine paths) at lines 18, 21, 24, etc. Two-path convention in the same file. Consolidation candidate.

11. **`commands/security-audit.md` and `commands/review.md` are minimal (11-12 LoC)** — both are essentially "delegate to {agent} with diff context". The thin-delegate pattern is intentional (per `commands/plan.md:7`'s framing of `/plan` as "a thin delegate to the planner agent"). Documentary observation.

12. **Command cross-reference patterns** — `verify-plan.md`, `research.md`, `build-plan.md`, `build-team.md` use relative paths (`../skills/X/SKILL.md`); other commands use repo-relative paths or external references. Documentary observation.

13. **`build-team.md:23` and `build-plan.md:14`+`build-plan.md:16` flag conventions** — `--force-route` / `--force-root` / `--force-plan` / `--skip-hets` / `--skip-fixes`. The flag namespaces are consistent with substrate scripts (`route-decide.js` accepts `--force-route` / `--force-root`). Documentary observation; alignment is intact.
